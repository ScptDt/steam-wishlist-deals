# Bitácora Operativa

Ultima actualizacion: 2026-05-11

## Proposito

Este archivo concentra el historial cronológico de avances, validaciones, hallazgos,
workarounds y evidencia operativa del proyecto.

`PENDIENTES.md` sigue siendo la fuente única de verdad para:
- pendientes activos
- prioridades
- estado actual
- proximo paso operativo

`BITACORA.md` NO reemplaza `PENDIENTES.md`.
Su función es servir como historial detallado y referencia operativa.

## Regla de uso

Mover aquí:
- avances cerrados o parciales que ya no cambian la prioridad del backlog
- evidencia cronológica por fecha
- workarounds, validaciones y notas históricas
- entradas largas que cargan demasiado `PENDIENTES.md`

Mantener en `PENDIENTES.md`:
- tracks activos
- quick wins activos
- bloqueos vigentes
- estado general
- siguiente paso operativo
- una bitácora reciente corta si todavía afecta decisiones actuales

## Formato de entrada

Usar `docs/runbooks/evidence-template.md` para nuevas entradas. Las notas operativas nuevas deben indicar `Autor/ejecutor: AudPen`; si AudPen audita o cierra trabajo iniciado por otro agente, separar `Autor/ejecutor original: <agente/no especificado>` de `Auditoría/cierre: AudPen`.

Formato compacto:

```markdown
- YYYY-MM-DD: Quick win <area/slice> <estado>. Autor/ejecutor: AudPen. <Objetivo>. Evidencia: `<comando>` (<resultado>), artefactos/logs: <ruta o resumen>. Incidencias: <breve>. Impacto/decisión: <qué cambia>. Siguiente seguimiento: <ninguno o próximo slice>.
```

Para Tracks, Quick Wins o tareas no triviales, registrar también el ciclo de vida:

```markdown
- YYYY-MM-DD: Inicio <Track/QW/tarea> <nombre>. Autor/ejecutor: AudPen. Objetivo: <resultado verificable>. Alcance: <archivos/áreas>. Fuera de alcance: <límites>. Validación prevista: <mínima proporcional>.
- YYYY-MM-DD: Cierre <Track/QW/tarea> <nombre>. Autor/ejecutor: AudPen. Resultado: <qué quedó>. Evidencia: <validación/resumen>. Incidencias: <breve>. Siguiente seguimiento: <ninguno o próximo slice>.
```

La nota de inicio puede omitirse en `BITACORA.md` para cambios triviales que no alteran backlog/estado operativo, pero debe quedar clara en conversación antes de ejecutar.

## Bitacora reciente de migracion

- 2026-04-24: `README.md` compacta el tail de referencia: flags comunes reemplazan la tabla completa, PAYDAY 2 queda como resumen con link a `payday2_guia.md`, y caché/config se condensan en notas de datos locales.
- 2026-04-24: `README.md` compacta `Mapa de módulos y entrypoints` hacia una tabla de superficies + resumen interno; `docs/project-rules.md` enlaza explícitamente el índice `docs/runbooks/README.md`.
- 2026-04-24: Se crea `docs/runbooks/README.md` como índice central de runbooks y `README.md` pasa a enlazar ese índice para desktop, performance y validaciones de features.
- 2026-04-24: Se crea `docs/runbooks/features-validation.md` para mover checklists de frontmatter Obsidian/Notion, `Tu Presupuesto Ideal` y share E2E fuera del README; `README.md` queda con resúmenes y links.
- 2026-04-24: `README.md` se alinea con la separación documental: compacta desktop/cross-platform hacia resumen + links a runbooks, corrige `Budget Mode` a `Tu Presupuesto Ideal` y actualiza formatos de salida Steam Deals.
- 2026-04-24: `PENDIENTES.md` normaliza pendientes abiertos con etiquetas `[Track]`, `[Quick Win]` o `[Futuro]` y notas compactas de Estado/Falta/Evidencia para facilitar cierre futuro.
- 2026-04-24: `PENDIENTES.md` compacta `Backlog de Features (Estado)` porque sus elementos estaban cerrados; queda un resumen de capacidades base y el detalle histórico permanece en esta bitácora.
- 2026-04-24: `PENDIENTES.md` compacta `Proximo Paso Operativo` a acción inmediata, bloqueo actual y regla de registro, evitando repetir estado P2/runbooks.
- 2026-04-24: `PENDIENTES.md` compacta `Bitacora reciente` a un resumen corto de entradas que aún afectan decisiones actuales; el detalle cronológico cerrado permanece en esta bitácora.
- 2026-04-24: `PENDIENTES.md` reemplaza `Plan inmediato (Windows / No bloqueado)` por `Trabajo no bloqueado`, dejando solo orientación y referencias a tracks activos para evitar duplicar backlog.
- 2026-04-24: `PENDIENTES.md` compacta `Plan Cross-Platform (Consolidado)` para dejar solo estado, criterio de cierre, fases y referencias; los comandos/checklists detallados permanecen en `docs/runbooks/desktop-*.md` y la evidencia larga en esta bitácora.
- 2026-04-24: `PENDIENTES.md` compacta la nota extensa de P3/modularización; la evidencia histórica de cortes, validaciones y comandos permanece en esta bitácora para no cargar el backlog vivo.
- 2026-04-23: Se crea `BITACORA.md` para sacar del backlog vivo la bitácora cronológica detallada y dejar `PENDIENTES.md` más enfocado en pendientes activos, prioridades y próximo paso operativo.

## Bitacora Cross-Platform por OS

| Fecha | Plataforma | Estado | Incidencias | Proximo paso |
|---|---|---|---|---|
| 2026-04-21 | Linux (entorno local) | pre-smoke final listo | `--warm-cache` headless completado con cache persistente y logs legibles en `~/.cache/steam_deals/logs/`; README y runbook Linux ya reflejan el flujo real y aclaran mejor `--vanity`. | Relanzar smoke largo del desktop con cache caliente y registrar `.md/.html/.csv` + cierre limpio. |
| 2026-04-17 | Linux (entorno local) | validacion manual avanzada | Sesion KDE nativa confirmada. `steam_tools_desktop.py --doctor` quedo READY en `.venv`; build local actualizado OK. Corrida larga real del desktop avanzo hasta `[8/11]` (`tags`) y ya habia escrito `.md`, `.html`, `share.html` y `.json`, pero detecto un bug real en el closeout final por mismatch de `smart_alerts`; el fix ya quedo validado con tests dirigidos. Se agregaron acciones UI para copiar/descargar logs, el desktop paso a usar cache persistente fuera de `_MEI` y se sumo `--warm-cache` headless con logs en carpeta `logs/`. | Dejar terminar warm-cache, rerun Linux final con cache caliente y confirmar `.csv` + cierre limpio sin procesos colgados. |
| 2026-04-16 | Decision operativa | Linux muy avanzado / macOS pospuesto | Se ejecuto validacion local fuerte en Linux durante esta iteracion; macOS sigue pendiente por no contar aun con host nativo disponible. CI cross-platform permanece OK como evidencia parcial base (`24487556896`). El smoke funcional Linux queda diferido porque la wishlist real supera 2K juegos y requiere una ventana amplia. | Ejecutar smoke funcional largo Linux cuando haya ventana suficiente; luego reactivar runbook macOS para cierre P2. |
| 2026-04-16 | Linux (entorno local) | validacion manual avanzada | `python3`/`pip` OK. `python3 -m pip install -r requirements-desktop.txt` fallo en system Python por PEP 668 (`externally-managed-environment`), por lo que se uso `.venv`. Build local OK con `dist/SteamToolsDesktop`. Primer arranque del artefacto: fallback web confirmado por falta de backend nativo (`qtpy`/`gi`). Tras instalar `pywebview[qt]` y rehacer build, se confirmo ventana nativa + server local en sesion grafica no-root usando `runuser` + `QT_QPA_PLATFORM=xcb`. El smoke funcional completo encontro un bug real de packaging (`ModuleNotFoundError: shared`) dentro del binario congelado; se corrigio `build_desktop.py`/spec agregando `--paths` + `collect_submodules(shared/renderers/app)` + hidden imports, y luego un check liviano del binario valido el primer evento `progress` (`Resolviendo Steam ID...`) del generator ya empaquetado. Warning actual a revisar: `libtiff.so.5` en empaquetado Qt. | Ejecutar run largo con wishlist real para confirmar outputs `.md/.html/.csv` y cierre limpio; decidir luego si `libtiff.so.5` requiere nota/paquete adicional. |
| 2026-04-16 | Linux (Ubuntu LTS) | validado en CI (parcial) | Workflow `Desktop Cross-Platform Validation` OK en `ubuntu-latest` (run `24487556896`): install deps, build desktop, `py_compile`, check fallback local y artifact `dist-ubuntu-latest` publicado. Falta validacion manual en host Linux nativo para ventana real, preflight funcional completo y cierre sin procesos colgados. | Ejecutar checklist manual Linux en host Ubuntu LTS y registrar incidencias/workarounds de backend nativo `pywebview`. |
| 2026-04-16 | macOS | validado en CI (parcial) | Workflow `Desktop Cross-Platform Validation` OK en `macos-latest` (run `24487556896`): install deps, build desktop, `py_compile`, check fallback local y artifact `dist-macos-latest` publicado. Falta validacion manual en host macOS para apertura de `.app`, quarantine/codesign/notarizacion segun distribucion. | Ejecutar checklist manual macOS (apertura local, quarantine, codesign) y registrar incidencias/workarounds. |

## Bitacora

- 2026-05-11: Inicio QW recomendaciones sociales/regalos / Gift Ideas en Web UI. Autor/ejecutor original: OpenCoder; Auditoría/cierre: AudPen. Objetivo: mostrar `gift_ideas` del último JSON dentro de `Acciones y recomendaciones del último reporte`, con razones sociales compactas cuando existan y sin crear una tarjeta principal nueva. Alcance: `web/steam_deals/app.js`, CSS mínimo y guardrails en `tests/test_web_assets.py`. Fuera de alcance: Share HTML, reportes generados, red real, `BG00G`, builds, checkout/carrito, tiendas externas, promo/cache/scoring global y reabrir recomendaciones personalizadas. Validación prevista: `tests.test_web_assets` y `git diff --check`.
- 2026-05-11: Cierre QW recomendaciones sociales/regalos / Gift Ideas en Web UI. Autor/ejecutor original: OpenCoder; Auditoría/cierre: AudPen. Resultado: `Resumen de tu última ejecución` muestra hasta 3 regalos dentro de `Acciones y recomendaciones del último reporte`, con descuento/precio, link seguro por AppID numérico, razones sociales compactas desde `social_reasons` con fallback local y copy explícito de que no abre carrito ni compra nada. Evidencia: `.venv/bin/python -m unittest tests.test_web_assets` (35 OK; warning benigno Steam 403/fallback público conocido) y `git diff --check` OK. Incidencias: ninguna. Siguiente seguimiento: Share HTML queda como única superficie pendiente si se prioriza; sin red real, `BG00G`, reportes generados, builds, checkout/carrito ni scoring global.
- 2026-05-11: Inicio QW recomendaciones sociales/regalos / razones visibles en outputs. Autor/ejecutor: OpenCoder. Objetivo: renderizar `social_reasons` de `gift_ideas` para que las recomendaciones de regalo expliquen por qué aparecen. Alcance: Markdown principal, HTML interactivo generado y fixtures determinísticas en `tests/test_generator_logic.py`. Fuera de alcance: red real, `BG00G`, reportes generados, builds, Web UI principal, Share HTML, checkout/carrito, tiendas externas, promo/cache/scoring global y reabrir recomendaciones personalizadas. Validación prevista: `py_compile` dirigido, `tests.test_generator_logic` y `git diff --check`.
- 2026-05-11: Cierre QW recomendaciones sociales/regalos / razones visibles en outputs. Autor/ejecutor: OpenCoder. Resultado: Gift Ideas en Markdown y HTML generado agregan columna `Por qué` cuando existen `social_reasons`, compactando hasta dos razones y escapando contenido HTML/Markdown. Evidencia: `.venv/bin/python -m py_compile renderers/markdown_renderer.py renderers/html_renderer.py tests/test_generator_logic.py`, `.venv/bin/python -m unittest tests.test_generator_logic` (252 OK) y `git diff --check` OK. Incidencias: ninguna. Siguiente seguimiento: Web UI principal/Share HTML solo si se prioriza; sin red real, `BG00G`, reportes generados, builds, checkout/carrito ni scoring global.
- 2026-05-11: Inicio QW recomendaciones sociales/regalos / señales post-run. Autor/ejecutor: AudPen. Objetivo: hacer que el post-run pase señales reales de compare (`overlap`) al helper de `gift_ideas` para preservar razones sociales y evitar repetir juegos en común cuando haya alternativas. Alcance: `app/steam_deals_engagement_post_run.py`, fixtures en `tests/test_generator_logic.py` y evidencia compacta. Fuera de alcance: red real, `BG00G`, reportes generados, builds, UI/renderers, checkout/carrito, tiendas externas, promo/cache/scoring global y reabrir recomendaciones personalizadas. Validación prevista: `py_compile` dirigido, `WatchlistTests`, `MatchingAndRecommendationTests` y `git diff --check`.
- 2026-05-11: Cierre QW recomendaciones sociales/regalos / señales post-run. Autor/ejecutor: AudPen. Resultado: `run_engagement_post_run` pasa `compare_data.overlap` como `overlap_appids` y propaga `friend_activity_games`/`friend_activity` si ya vienen en el compare local, manteniendo la firma legacy del helper y sin nuevas fuentes de datos. Evidencia: `.venv/bin/python -m py_compile app/steam_deals_engagement_post_run.py tests/test_generator_logic.py`, `.venv/bin/python -m unittest tests.test_generator_logic.WatchlistTests tests.test_generator_logic.MatchingAndRecommendationTests` (14 OK) y `git diff --check` OK. Incidencias: ninguna. Siguiente seguimiento: Web UI principal/Share HTML solo si se prioriza; render Markdown/HTML ya cubierto en slice separado. Sin red real, `BG00G`, reportes generados, builds, checkout/carrito ni promo/cache/scoring.
- 2026-05-11: Inicio QW recomendaciones sociales/regalos / helper puro inicial. Autor/ejecutor: AudPen. Objetivo: definir una base offline para que `gift_ideas` no repita overlap cuando haya alternativas y pueda explicar recomendaciones con señales simples. Alcance: `app/steam_deals_recommendations.py`, wrapper público en `steam_deals_generator.py`, fixtures en `tests/test_generator_logic.py` y evidencia en `PENDIENTES.md`. Fuera de alcance: red real, `BG00G`, reportes generados, builds, UI/renderers, checkout/carrito, tiendas externas, integración completa de compare/actividad y recalibración global. Validación prevista: `py_compile`, `MatchingAndRecommendationTests` y `git diff --check`.
- 2026-05-11: Cierre QW recomendaciones sociales/regalos / helper puro inicial. Autor/ejecutor: AudPen. Resultado: `build_gift_ideas` sigue aceptando la firma existente y ahora puede recibir `overlap_appids`/`friend_activity_games` para devolver copias con `social_reasons` y `social_signals`; cuando hay alternativas no-overlap evita repetir juegos ya en común, y si solo hay overlap conserva fallback con razón explícita. Evidencia: `.venv/bin/python -m py_compile app/steam_deals_recommendations.py steam_deals_generator.py tests/test_generator_logic.py`, `.venv/bin/python -m unittest tests.test_generator_logic.MatchingAndRecommendationTests` (9 OK) y `git diff --check` OK. Incidencias: ninguna. Siguiente seguimiento: integrar señales reales del compare/post-run y renderizar razones en outputs como slice separado; sin red real, `BG00G`, reportes generados, builds, UI/renderers ni checkout/carrito.
- 2026-05-11: Inicio QW warm-cache / cobertura parcial y continuación UI. Autor/ejecutor: AudPen. Objetivo: reforzar que la UI/último reporte comunique cobertura parcial y que `Continuar warm-cache` reutiliza la caché actual con `--warm-cache`, sin sugerir ni usar `--no-cache`. Alcance: copy/guardrails Web y tests dirigidos de assets/command builder. Fuera de alcance: red real, `BG00G`, cold-cache, reportes generados, builds, background jobs, promo-cache, PAYDAY 2 y recomendaciones. Validación prevista: `py_compile` dirigido, `tests.test_web_assets`, `tests.test_generated_files_serving` y `git diff --check`.
- 2026-05-11: Cierre QW warm-cache / cobertura parcial y continuación UI. Autor/ejecutor: AudPen. Resultado: el aviso `Caché parcial` indica que `Continuar warm-cache` reusa la misma caché en una corrida normal con `--warm-cache` y sin `--no-cache`; los tests del command builder refuerzan que `--no-cache` solo aplica a corridas no warm-cache. Evidencia: `.venv/bin/python -m py_compile steam_deals_web.py tests/test_web_assets.py tests/test_generated_files_serving.py`, `.venv/bin/python -m unittest tests.test_web_assets tests.test_generated_files_serving` (73 OK; warning benigno Steam 403/fallback público conocido) y `git diff --check` OK. Incidencias: ninguna. Siguiente seguimiento: background/banner interno y `active_offer_until` siguen futuros; sin red real, `BG00G`, cold-cache, reportes generados ni builds.
- 2026-05-11: Inicio QW warm-cache / continuación CLI accionable. Autor/ejecutor: OpenCoder. Objetivo: hacer que el resumen offline indique cómo continuar pendientes/diferidos con el mismo cache dir y `--warm-cache`, evitando `--no-cache` salvo benchmark aprobado. Alcance: copy de `app/steam_deals_warm_cache_summary.py` y tests determinísticos. Fuera de alcance: red real, `BG00G`, cambio de política de cache, background jobs, promo-cache, reportes generados y builds. Validación prevista: `tests.test_warm_cache_summary`, `py_compile` dirigido y `git diff --check`.
- 2026-05-11: Cierre QW warm-cache / continuación CLI accionable. Autor/ejecutor: OpenCoder. Resultado: la línea `Continuación sugerida` del resumen offline ahora pide conservar el mismo cache dir, repetir una corrida normal con `--warm-cache` desde `next_resume_hint` y no usar `--no-cache` salvo benchmark aprobado. Evidencia: `.venv/bin/python -m py_compile app/steam_deals_warm_cache_summary.py steam_deals_warm_cache_summary.py tests/test_warm_cache_summary.py`, `.venv/bin/python -m unittest tests.test_warm_cache_summary` (24 OK) y `git diff --check` OK. Incidencias: ninguna. Siguiente seguimiento: background/banner interno sigue futuro; sin red real, `BG00G`, reportes generados, builds ni cache policy.
- 2026-05-08: Cierre docs-only / track recomendaciones personalizadas. Autor/ejecutor: AudPen. Resultado: `PENDIENTES.md` marca `Recomendaciones personalizadas por actividad y biblioteca` como cerrado/pausado salvo fricción visual real o señales locales nuevas, y actualiza el item activo hacia `Recomendaciones sociales y regalos` como siguiente foco producto recomendado con fixtures offline si se aprueba. Evidencia: revisión documental de `PENDIENTES.md`/`BITACORA.md` y `git diff --check`. Incidencias: ninguna. Siguiente seguimiento: proponer readiness/helper puro para recomendaciones sociales/regalos antes de tocar código; sin red real, `BG00G`, reportes generados, builds, checkout/carrito real ni colecciones ricas nuevas.
- 2026-05-08: Cierre revisión visual local `Evalúa mi selección`. Autor/ejecutor: AudPen. Resultado: fixture local en Firefox headless confirma que el panel permanece dentro de `Acciones y recomendaciones del último reporte`, permite candidatos marcados + AppIDs/URLs pegados, muestra resumen y tarjetas `Conservar`/`Dudar`/`Quitar` con razones compactas, y mantiene el copy de simulador local sin carrito/compra. Evidencia: fixture `/tmp/selection-review-manual-20260508-adolfo/selection-review-visual.html`, screenshot `/tmp/selection-review-manual-20260508-adolfo/selection-review-visual.png`, revisión estática de `web/steam_deals/app.js`/`app.css` y tests existentes de assets/selection-review. Incidencias: Firefox como root falló por sesión gráfica, `runuser` no existe; se reintentó con `su adolfo` y directorio temporal escribible. Siguiente seguimiento: Windows real queda pendiente solo si se requiere validar host/browser Windows; sin red real, `BG00G`, reportes generados, builds, checkout/carrito real ni recalibración.
- 2026-05-08: Inicio QW Alertas inteligentes v2 / guardrails determinísticos. Autor/ejecutor: OpenCoder. Objetivo: reforzar fixtures offline de conteos de alertas para límites de umbral, datos malformados y estabilidad del contrato sin calibrar umbrales con corridas reales. Alcance: `app/steam_deals_alerts.py` y `SmartAlertsTests`. Fuera de alcance: red real, `BG00G`, reportes generados, builds, UI/renderers, calibración real de umbrales y cierre completo del track. Validación prevista: `SmartAlertsTests`, `py_compile` dirigido y `git diff --check`.
- 2026-05-08: Cierre QW Alertas inteligentes v2 / guardrails determinísticos. Autor/ejecutor: OpenCoder. Resultado: los conteos aceptan strings numéricos seguros, ignoran entradas malformadas sin romper, cubren límites exactos de score/subida/margen global y solo cuentan juegos con bundles válidos. Evidencia: `.venv/bin/python -m py_compile app/steam_deals_alerts.py tests/test_generator_logic.py`, `.venv/bin/python -m unittest tests.test_generator_logic.SmartAlertsTests` (5 OK) y `git diff --check` OK. Incidencias: ninguna. Siguiente seguimiento: calibración real/larga de umbrales queda pendiente para cerrar el track; sin red real, `BG00G`, reportes generados, builds ni UI/renderers.
- 2026-05-08: Inicio QW PAYDAY 2 / copy refresh normal vs forzado. Autor/ejecutor: AudPen. Objetivo: aclarar en la UI cuándo usar `Actualizar datos` (respeta caché/TTL) y cuándo usar `Forzar catálogo` (`--no-cache` para DLC nuevo o caché viejo) sin cambiar comportamiento. Alcance: copy Web PAYDAY 2 y tests/assets. Fuera de alcance: red real, `BG00G`, hardcodear DLCs, cambiar TTL/cache policy, diagnóstico Web nuevo, ownership manual, reportes generados, builds y smokes manuales. Validación prevista: `py_compile` dirigido, `tests.test_web_assets` y `git diff --check`.
- 2026-05-08: Cierre QW PAYDAY 2 / copy refresh normal vs forzado. Autor/ejecutor: AudPen. Resultado: la UI PAYDAY 2 distingue `Actualizar datos` como refresh normal con caché/TTL y `Forzar catálogo` como `--no-cache` solo para DLC nuevo/caché viejo; el copy aclara que no inventa DLCs que Steam no expone ni borra marcados manuales. Evidencia: `.venv/bin/python -m py_compile payday2_web.py tests/test_web_assets.py`, `.venv/bin/python -m unittest tests.test_web_assets` (34 OK, warning benigno Steam 403/fallback público) y `git diff --check` OK. Incidencias: ninguna. Siguiente seguimiento: usar diagnóstico por appid/nombre si un DLC esperado sigue sin aparecer.
- 2026-05-08: Inicio QW promo activa / hardening de clasificador. Autor/ejecutor: OpenCoder. Objetivo: reforzar fixtures offline para que el clasificador preserve la jerarquía conservadora cuando títulos mezclan eventos grandes y palabras rutinarias (`Weekend`, `Weeklong`, `Deal`). Alcance: `app/steam_deals_steam_api.py` y `SteamAdapterTests`. Fuera de alcance: red real, `BG00G`, picks/cache por promo, invalidación de cache, renderers/UI, reportes generados, builds y recalibración de scoring. Validación prevista: `SteamAdapterTests`, `py_compile` dirigido y `git diff --check`.
- 2026-05-08: Cierre QW promo activa / hardening de clasificador. Autor/ejecutor: OpenCoder. Resultado: el clasificador prioriza señales de mega sale/fest/publisher/franchise antes de palabras rutinarias en títulos mixtos y el contexto conserva orden estable cuando dos promos tienen misma prioridad/tipo. Evidencia: `.venv/bin/python -m py_compile app/steam_deals_steam_api.py tests/test_generator_logic.py`, `.venv/bin/python -m unittest tests.test_generator_logic.SteamAdapterTests` (21 OK) y `git diff --check` OK. Incidencias: ninguna. Siguiente seguimiento: investigar fuente oficial de tipos/nombres Steam antes de ampliar listas o tocar cache promo-aware.
- 2026-05-08: Inicio QW promo activa / fixtures conservadoras. Autor/ejecutor: AudPen. Objetivo: reforzar con fixtures offline que `Contexto de promo activa` mantenga prioridad/copy conservador para promos simultáneas sin prometer predicción ni cambiar score. Alcance: clasificación/copy de `active_promo_context` y tests dirigidos si aparece brecha. Fuera de alcance: recomendaciones personalizadas, red real, `BG00G`, picks/cache por promo, invalidación de cache, reportes generados, builds y recalibración de scoring. Validación prevista: tests dirigidos de promo/Web assets + `git diff --check`.
- 2026-05-08: Cierre QW promo activa / fixtures conservadoras. Autor/ejecutor: AudPen. Resultado: Markdown y HTML generado ahora muestran copy explícito de contexto local/no predictivo junto a `decision_hint` y `simultaneous_hint` cuando el JSON los trae, alineando reportes generados con la Web UI sin tocar clasificación, scoring ni cache. Evidencia: `.venv/bin/python -m py_compile renderers/markdown_renderer.py renderers/html_renderer.py tests/test_generator_logic.py tests/test_web_assets.py`, `.venv/bin/python -m unittest tests.test_generator_logic.SteamAdapterTests tests.test_generator_logic.RunOutputTests tests.test_web_assets.WebAssetsTests` (52 OK) y `git diff --check` OK. Incidencias: ninguna. Siguiente seguimiento: investigar fuente oficial de nombres/tipos Steam como slice separado antes de hardcodear listas largas o tocar promo-cache.
- 2026-05-08: Inicio QW trend / Historial local accionable. Autor/ejecutor original: no especificado; Auditoría: AudPen. Objetivo: confirmar o ajustar que `trend` aparezca solo como historial local útil cuando aporte una señal accionable, sin lenguaje predictivo. Alcance: gating/copy de renderers y tests dirigidos si hace falta. Fuera de alcance: recomendaciones personalizadas, Dashboard histórico, forecasting, red real, `BG00G`, reportes generados, builds y smokes manuales. Validación prevista: revisión estática + tests dirigidos de renderer/lógica y `git diff --check` si hay cambios.
- 2026-05-08: Cierre QW trend / Historial local accionable. Autor/ejecutor original: no especificado; Auditoría/cierre: AudPen. Resultado: el HTML generado ahora muestra `Historial local`, sparkline y salto `Ver historial` solo cuando los snapshots locales tienen movimiento real de precio; snapshots planos/repetidos quedan ocultos para evitar ruido y se conserva copy no predictivo. Evidencia: `.venv/bin/python -m py_compile renderers/html_renderer.py tests/test_generator_logic.py`, `.venv/bin/python -m unittest tests.test_generator_logic.RunOutputTests` (6 OK) y `git diff --check` OK. Incidencias: ninguna. Siguiente seguimiento: mantener `trend` como señal secundaria; solo reabrir si revisión visual detecta ruido o falta una señal local igual de clara.
- 2026-05-08: Inicio QW promo activa / contexto comprar ahora vs esperar. Objetivo: completar el contexto de promos simultáneas con hints conservadores para interpretar si la promo destacada amerita revisar ahora o tratarse como promo rutinaria. Alcance: campos derivados en `active_promo_context`, copy Web UI y tests dirigidos. Fuera de alcance: picks/cache por promo, cache policy, red real, `BG00G`, reportes generados, builds, normalización oficial de APIs Steam y recalibración de scoring. Validación prevista: tests determinísticos de promo/Web assets + `git diff --check`.
- 2026-05-08: Cierre QW promo activa / contexto comprar ahora vs esperar. Resultado: `active_promo_context` agrega `decision_hint` por categoría (`major_sale`, `fest`, `publisher_sale`, promos rutinarias, launch/weeklong) y `simultaneous_hint` cuando hay promos simultáneas; la Web UI muestra esos hints bajo `Contexto de promo activa` sin presentarlos como predicción ni cambio de score. Evidencia: `.venv/bin/python -m py_compile app/steam_deals_steam_api.py tests/test_generator_logic.py tests/test_web_assets.py`, `.venv/bin/python -m unittest tests.test_generator_logic.SteamAdapterTests tests.test_web_assets.WebAssetsTests` (46 OK) y `git diff --check` OK. Incidencias: ninguna. Siguiente seguimiento: investigar fuente oficial de nombres/tipos Steam como slice separado antes de hardcodear listas largas o tocar promo-cache.
- 2026-05-08: Inicio Track Recomendaciones personalizadas / resumen de actividad local. Objetivo: exponer un resumen compacto de actividad reciente y juegos más jugados usando señales locales ya disponibles (`playtime_2weeks`, `playtime_forever`, `hours_played`/`hours`) dentro del perfil de recomendaciones personalizadas. Alcance: helper puro, chips de perfil existentes y tests determinísticos. Fuera de alcance: red real, `BG00G`, nuevas APIs/stores, colecciones ricas, checkout/carrito, recalibración global, reportes generados y builds. Validación prevista: tests dirigidos de recomendaciones/renderers/Web assets + `git diff --check`.
- 2026-05-08: Cierre QW recomendaciones personalizadas / normalización de etiquetas equivalentes. Resultado: el matching de estilos usa clave canónica para unir variantes de mayúsculas, espacios, guiones y underscores (`Single-player`/`Singleplayer`, `Action_RPG`/`Action RPG`) sin cambiar superficies ni recalibrar score global; conserva labels visibles originales en perfil y razones. Evidencia: `.venv/bin/python -m py_compile app/steam_deals_recommendations.py tests/test_generator_logic.py`, `.venv/bin/python -m unittest tests.test_generator_logic.PersonalizedRecommendationsTests` (6 OK) y `git diff --check` OK. Incidencias: ninguna. Siguiente seguimiento: normalizar señales reales de actividad reciente/más jugados desde datos locales si se prioriza el siguiente slice del track.
- 2026-05-08: Inicio QW recomendaciones personalizadas / señales locales de horas. Objetivo: alinear el peso de actividad con el resumen local para que `hours_played`/`hours`, `playtime_2weeks` y `playtime_forever` se interpreten de forma consistente en perfil y términos de actividad. Alcance: helper puro de normalización de horas, chips en superficies existentes de recomendaciones personalizadas y tests determinísticos. Fuera de alcance: red real, `BG00G`, reportes generados, builds, nuevas superficies UI, warm-cache/background y recalibración global de score. Validación prevista: `PersonalizedRecommendationsTests`, renderers/Web assets dirigidos, `py_compile` y `git diff --check`.
- 2026-05-08: Cierre QW recomendaciones personalizadas / señales locales de horas. Resultado: `app/steam_deals_recommendations.py` centraliza la normalización de horas locales y reutiliza `hours` junto a `hours_played`, `playtime_2weeks` y `playtime_forever` para ponderar términos de actividad y armar `activity_summary`; Markdown, HTML, Share HTML y Web UI muestran chips compactos de actividad local/más jugado dentro del perfil personalizado existente. Rework aplicado: la auditoría detectó que `library_games.hours` podía inflar `activity_terms` y generar razón `encaja con tu actividad reciente` aunque `activity_summary.tracked_count=0`; se corrigió para no copiar campos de horas desde biblioteca y para construir términos de actividad solo con playtime local positivo. Evidencia: `.venv/bin/python -m py_compile app/steam_deals_recommendations.py renderers/html_renderer.py renderers/markdown_renderer.py renderers/share_html_renderer.py tests/test_generator_logic.py tests/test_web_assets.py`, `.venv/bin/python -m unittest tests.test_generator_logic.PersonalizedRecommendationsTests tests.test_generator_logic.RunOutputTests tests.test_generator_logic.RankTopPicksTests tests.test_web_assets.WebAssetsTests` (89 OK) y `git diff --check` OK. Incidencias: rework fix-forward acotado por mezcla de horas biblioteca/actividad real. Siguiente seguimiento: análisis de biblioteca por distribución de géneros si aporta valor; sin red real, `BG00G`, reportes generados, builds ni superficie UI nueva.
- 2026-05-08: Inicio QW recomendaciones personalizadas / distribución de géneros de biblioteca. Objetivo: exponer una distribución compacta de géneros/estilos de biblioteca dentro del perfil personalizado usando datos locales ya presentes. Alcance: helper puro en recomendaciones, campo de perfil interno/JSON y tests determinísticos. Fuera de alcance: red real, `BG00G`, reportes generados, builds, UI nueva, colecciones ricas, Fanatical/multi-store, warm-cache/background y recalibración global. Validación prevista: `PersonalizedRecommendationsTests`, `py_compile` dirigido y `git diff --check`.
- 2026-05-08: Cierre QW recomendaciones personalizadas / distribución de géneros de biblioteca. Resultado: `library_summary` agrega `genre_distribution` y `genre_coverage_count` derivados de géneros/tags locales, con conteo canónico por juego para no duplicar equivalentes (`Action-RPG`/`Action RPG`) ni juegos sin metadata. Evidencia: `.venv/bin/python -m py_compile app/steam_deals_recommendations.py tests/test_generator_logic.py`, `.venv/bin/python -m unittest tests.test_generator_logic.PersonalizedRecommendationsTests` (10 OK) y `git diff --check` OK. Incidencias: ninguna. Siguiente seguimiento: solo refinamientos si revisión visual real muestra fricción o aparece una señal local nueva de valor claro; sin red real, `BG00G`, reportes generados, builds, UI nueva, colecciones ricas ni recalibración global.
- 2026-05-08: Inicio QW docs-only / cierre de pendientes stale Windows. Objetivo: alinear `PENDIENTES.md` para no repetir quick wins ya implementados sobre filtros avanzados, Budget Mode sin filas, Copiar log y logs sanitizados. Alcance: backlog/evidencia documental. Fuera de alcance: código runtime, warm-cache Tracks, red real, `BG00G`, reportes generados, builds y smokes desktop. Validación prevista: revisión documental + `git diff --check`.
- 2026-05-08: Cierre QW docs-only / cierre de pendientes stale Windows. Resultado: se marcaron como cerrados los QW stale de `Filtros avanzados`, `Tu Presupuesto Ideal` sin filas, fallback seguro de `Copiar log` y explicación de logs sanitizados, apuntando a evidencia/commits/tests existentes. Evidencia: markers locales revisados en `web/steam_deals/index.html`, `renderers/html_renderer.py`, `steam_tools_desktop.py`, `tests.test_web_assets`, `tests.test_generator_logic` y `tests.test_desktop_share`; `git diff --check` OK. Incidencias: ninguna. Siguiente seguimiento: continuar con pendientes realmente abiertos; no se tocó warm-cache ni runtime.
- 2026-05-08: Inicio QW Track Performance / estados legibles de caché. Objetivo: derivar etiquetas compactas (`fresh`, `stale usable`, `pending/deferred`, `cooldown`, `failed/no data`, `missing`) desde campos existentes de caché sin persistir metadata nueva. Alcance: helper puro de clasificación, conteos de presentación si aplica y tests determinísticos. Fuera de alcance: migración de schema, `active_offer_until`, promo-cache, red real, `BG00G`, `--no-cache`, reportes generados, builds y background. Validación prevista: tests puros de clasificación + assets/UI si se toca Web UI + `git diff --check`.
- 2026-05-08: Cierre QW Track Performance / estados legibles de caché. Resultado: se agregó un clasificador puro de estados derivados en `app/steam_deals_cache_policy.py`, el resultado de precios expone `cache_state_counts`/`cache_state_summary` en `cache_coverage`, y el aviso Web `Caché parcial` muestra chips compactos `fresh cache`, `stale usable`, `pending/deferred`, `failed/cooldown`, `failed/no data` y `missing` cuando existen. Evidencia: `.venv/bin/python -m py_compile app/steam_deals_cache_policy.py steam_deals_generator.py tests/test_generator_logic.py tests/test_web_assets.py` OK, `.venv/bin/python -m unittest tests.test_generator_logic.PriceCacheTests tests.test_generator_logic.RunOutputTests tests.test_web_assets.WebAssetsTests` (70 OK) y `git diff --check` OK. Incidencias: ninguna. Siguiente seguimiento: background/banner interno o expiración por fin de oferta quedan futuros; sin schema migration, red real, `BG00G`, `--no-cache`, reportes generados ni builds.
- 2026-05-08: Inicio QW promo activa / prioridad y copy de promos simultáneas. Objetivo: evitar que `Now Available`/launch se lea como promo más importante que un Steam Fest o mega sale cuando hay promos simultáneas. Alcance: clasificación/prioridad de `active_promo_context`, copy Web UI y tests dirigidos. Fuera de alcance: picks/cache por promo, cache policy, red real, `BG00G`, reportes generados, builds y recalibración de scoring. Validación prevista: tests determinísticos de promo/Web assets + `git diff --check`.
- 2026-05-08: Cierre QW promo activa / prioridad y copy de promos simultáneas. Resultado: `active_promo_context` ahora elige promo destacada por jerarquía antes de orden/cantidad, clasifica `Now Available` como `launch` y la Web UI cambia `Promo principal` por copy menos ambiguo (`Promo detectada con más peso`, `Promo destacada`). Evidencia: `.venv/bin/python -m py_compile app/steam_deals_steam_api.py renderers/html_renderer.py renderers/markdown_renderer.py tests/test_generator_logic.py tests/test_web_assets.py`, tests dirigidos `SteamAdapterTests` + `WebAssetsTests` OK y `git diff --check` OK. Incidencias: ninguna. Siguiente seguimiento: el track de promo activa sigue abierto para picks/cache por promo y contexto comprar ahora vs esperar.
- 2026-05-08: Inicio QW Track Performance / Continuar warm-cache. Objetivo: agregar una acción explícita para continuar la revalidación de pendientes usando la caché actual, sin `--no-cache` ni relanzar una wishlist completa. Alcance: definir/implementar la superficie inicial CLI/Web/Desktop compatible con el flujo existente, respetando lock/409 si ya hay ejecución activa y limitando el trabajo a pendientes/stale/cooldown vencido. Fuera de alcance: background jobs, daemon, notificaciones, TTL/promo-cache, `BG00G` live, cold-cache largo, builds y reportes generados. Validación prevista: tests de command builder/estado/lock y assets/UI si toca Web UI; sin red real.
- 2026-05-08: Cierre QW Track Performance / Continuar warm-cache. Resultado: el aviso `Caché parcial` del último reporte agrega botón `Continuar warm-cache`, que lanza el flujo `/api/run` existente con `warm_cache=true`, `no_cache=false`, progreso SSE y lock/409 heredado; el backend traduce la acción a `--warm-cache` y bloquea `--no-cache` aunque el checkbox estuviera activo. Evidencia: `.venv/bin/python -m py_compile steam_deals_web.py tests/test_generated_files_serving.py tests/test_web_assets.py` OK, `.venv/bin/python -m unittest tests.test_generated_files_serving tests.test_web_assets` (72 OK) y `git diff --check` OK. Incidencias: warning benigno conocido de Steam 403/fallback público durante tests. Siguiente seguimiento: estados legibles de caché si se prioriza; no hubo red real, `BG00G`, cold-cache largo, `--no-cache`, background, reportes generados ni builds.
- 2026-05-08: Backlog promo activa / jerarquía de prioridades registrada. A partir del screenshot `/tmp/Spectacle.xEakTW/Screenshot_20260508_112322.png` se detectó que `Promo principal: Now Available - Everything is Crab` puede comunicar más prioridad que `Steam Deckbuilders Fest 2026`, aunque un Fest temático debería pesar más que launch/weeklong/weekend y quedar solo por debajo de mega sales oficiales. Decisión: ordenar promos por jerarquía antes de cantidad/orden (`Spring/Summer/Autumn/Winter` > Steam Fests temáticos > Publisher/Franchise Sales > Weekend > Launch/Now Available > Weeklong > Other/Unknown) y crear QW para ajustar copy/prioridad visible; además queda nota de investigar nombres/tipos oficiales desde APIs/endpoints públicos de Steam antes de hardcodear listas largas. Evidencia: conversación de producto + screenshot local; docs-only, sin red real, reportes generados, `BG00G`, builds ni cambios de scoring/cache.
- 2026-05-08: Inicio QW Track Performance / cobertura warm-cache visible. Objetivo: mostrar al usuario cuando la corrida deja cobertura parcial (`processed/deferred`) y evitar que las ofertas mostradas parezcan cobertura total. Alcance: contrato mínimo de reporte/Web UI con métricas existentes, copy y tests dirigidos. Fuera de alcance: `Continuar warm-cache`, background jobs, `--no-cache`, red real, `BG00G` live, builds, reportes generados y cambios TTL/promo-cache. Validación prevista: tests determinísticos de presentación/assets/generator según superficie + `git diff --check`.
- 2026-05-08: Cierre QW Track Performance / cobertura warm-cache visible. Resultado: `steam_deals_generator.py` deriva `cache_coverage` desde el resultado de precios y `renderers/json_renderer.py` lo serializa en el JSON; la Web UI del último reporte muestra un aviso `Caché parcial` con cobertura revisada, pendientes por confirmar y copy de que las ofertas pueden no incluir juegos aún no verificados. Evidencia: `.venv/bin/python -m py_compile steam_deals_generator.py renderers/json_renderer.py tests/test_generator_logic.py tests/test_web_assets.py` OK, `.venv/bin/python -m unittest tests.test_generator_logic.RunOutputTests tests.test_web_assets.WebAssetsTests` (33 OK) y `git diff --check` OK. Incidencias: ninguna. Siguiente seguimiento: `Continuar warm-cache` queda como QW posterior con guardrail de lock/409, sin background ni `--no-cache`.
- 2026-05-08: Evidencia Linux source-mode fallback web por pywebview ausente registrada. Al ejecutar `python3 steam_tools_desktop.py` sin módulo `webview` disponible, el launcher levantó la Web UI local en `http://127.0.0.1:8080` y degradó correctamente a navegador, imprimiendo `URL fallback: http://127.0.0.1:8080?desktop_fallback=1&reason=missing-webview`. Incidencias: backend nativo ausente esperado en ese entorno source; se evita pegar el traceback completo. Impacto/decisión: suma evidencia real del fallback automático no forzado en Linux source mode, pero no sustituye cierre de binario nativo, Windows/macOS visual ni host macOS.
- 2026-05-08: Inicio QW docs-only proceso inicio/cierre de Tracks/QW. Objetivo: formalizar que cada Track, Quick Win o tarea no trivial deje nota breve de inicio y cierre. Alcance: `PENDIENTES.md`, `BITACORA.md`, `docs/runbooks/evidence-template.md`. Fuera de alcance: código runtime, tests funcionales, red real, builds y reportes generados. Validación prevista: revisión documental + `git diff --check`.
- 2026-05-08: Cierre QW docs-only proceso inicio/cierre de Tracks/QW. Resultado: `PENDIENTES.md` define la regla operativa, `BITACORA.md` agrega plantilla de inicio/cierre y `docs/runbooks/evidence-template.md` queda alineado para registrar ciclos de vida sin logs largos. Evidencia: `git diff --check` OK. Incidencias: ninguna. Siguiente seguimiento: aplicar la regla en el próximo Track/QW no trivial.
- 2026-05-08: Tracks/QW ajustan matices del backlog warm-cache incremental. Se precisó que el primer quick win de cobertura visible no debe tocar schema de caché/precios ni política de fetching; si Web UI/reporte no recibe métricas de cobertura, puede derivarlas del último warm-cache log o exponer un status mínimo/campo de presentación. También se agregó que `Continuar warm-cache` debe bloquear concurrencia con `/api/run`/warm-cache activo o responder 409/estado claro, no usar `--no-cache`, no relanzar wishlist completa y limitarse a pendientes/stale/cooldown vencido; background queda como futuro por locks, estado de proceso, polling/SSE y UX de notificación. Evidencia: docs-only aprobado por usuario; `git diff --check` OK. Impacto/decisión: QW puede tomar cobertura visible sin abrir arquitectura, y el track mantiene background/locks como fase posterior.
- 2026-05-08: Backlog Track Performance / warm-cache incremental registrado. Tras revisar el warm-cache real de `BG00G` (`processed=360`, `deferred=2,575`, `exhausted=true`) se decidió separar el problema en un track de UX/performance incremental: conservar la velocidad del presupuesto resumible, pero mostrar cobertura parcial de forma explícita, permitir continuar solo pendientes/desconocidos/stale relevantes y evitar que `deals actuales` sugiera cobertura completa. Razonamiento: no conviene llamarlo blacklist permanente; encaja mejor como cola resumible + skip temporal por frescura, reutilizando lo ya existente (`next_resume_hint`, cache parcial, failure cooldown, stale-while-revalidate) antes de persistir nuevos estados como `fresh_checked`, `no_offer_checked_at` o `active_offer_until`. Slices registrados en `PENDIENTES.md`: cobertura real visible, acción `Continuar warm-cache`, etiquetas legibles de estado de caché, background con banner interno y expiración por fecha fin de oferta solo si el dato es confiable. Evidencia: análisis documental del log/runbook y conversación de producto; no se ejecutaron comandos de red, `BG00G`, cold-cache, reportes generados, builds ni cambios de implementación. Impacto/decisión: queda listo para que la IA de Tracks/Quick Wins tome el primer slice sin mezclar background job, notificaciones ni cambio de política TTL.
- 2026-05-08: Quick win Track Performance / semántica de diferidos warm-cache cerrado. El resumen offline ahora marca cobertura parcial cuando `deferred_by_time_budget > 0`, separa juegos revalidados vs pendientes no revalidados, muestra deals encontrados con la cobertura disponible, aclara que no se sabe aún si los pendientes tienen oferta, convierte `next_resume_hint` en continuación sugerida y distingue velocidad de completitud; el runbook warm-cache agrega la misma interpretación y leyenda de estados (`processed`, `deferred`, `fresh cache`, `stale cache`, `failed/cooldown`). Evidencia: `.venv/bin/python -m py_compile app/steam_deals_warm_cache_summary.py steam_deals_warm_cache_summary.py tests/test_warm_cache_summary.py` OK, `.venv/bin/python -m unittest tests.test_warm_cache_summary` (24 OK) y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: el log con `processed=360`, `deferred=2,575`, `wishlist=2,935` y `22 deals` debe leerse como `360/2,935` revalidados y `22 deals encontrados con cobertura parcial`, no como cobertura completa ni como prueba de que los diferidos no tengan ofertas; sin red real, `BG00G`, cold-cache largo, reportes generados ni builds.
- 2026-05-07: Hallazgos Windows manuales registrados como backlog. Se clasificaron reportes de la prueba Windows: `st1.jpeg` muestra exceso de información en `Filtros avanzados`; `st2.jpeg`/`st3.jpeg`/`st4.jpeg` muestran `Tu Presupuesto Ideal` con variantes/totales pero tablas sin filas; `Copiar log` en desktop source mode falló con traceback pywebview por falta de PyQt6 antes de mostrar fallback; y el log descargado muestra placeholders `[Redactado]`/`[Ruta]` que son esperados por seguridad pero necesitan mejor explicación. Evidencia: screenshots locales `/home/adolfo/Downloads/st1.jpeg` a `st4.jpeg`, error resumido por el tester y docs actuales de pywebview consultadas en `.tmp/external-context/pywebview/windows-backend-clipboard.md`. Incidencias: no se recibió el log descargado, así que no se versiona ni se analiza contenido sensible. Impacto/decisión: se agregan Quick Wins separados en `PENDIENTES.md` para compactar filtros, diagnosticar Budget Mode, mejorar fallback de clipboard Windows y aclarar logs sanitizados; no se toca código, dependencias, red real, `BG00G`, builds ni reportes generados.
- 2026-05-07: Revisión visual P1 UX principal + Dashboard histórico cerrada. Se levantó la Web UI local sin ejecutar reportes y se capturó evidencia en Firefox headless para confirmar que el CTA `Generar reportes` domina antes de salida, `Alertas de precio` e histórico; en móvil aproximado queda apilado pero antes de secciones secundarias, y el Dashboard histórico abierto con fixture local muestra búsqueda/filtros/orden, `Comparar 2 recientes`, resumen visual, deltas, tendencia, contexto e `Historial por juego` con `Ver más juegos` sin requerir vista dedicada. Evidencia: screenshots `/tmp/opencode/visual-hierarchy-review/main-desktop.png`, `/tmp/opencode/visual-hierarchy-review/main-mobile-tall-wait.png`, `/tmp/opencode/visual-hierarchy-review/history-open-fixture-wait.png`; validación previa `.venv/bin/python -m unittest tests.test_web_assets` (30 OK, warning benigno Steam 403/fallback público), `.venv/bin/python -m unittest tests.test_history_drilldown_assets` (2 OK), `.venv/bin/python -m unittest tests.test_track_history_flow` (14 OK) y `git diff --check` OK. Incidencias: primer intento de Firefox falló por `XAUTHORITY` de usuario; se reintentó aprobado con entorno headless aislado en `/tmp/opencode`. Impacto/decisión: se cierran los tracks P1 de jerarquía visual principal y Dashboard histórico HTML; no se ejecutaron reportes, apertura/descarga de artefactos, recomendaciones personalizadas, metadata de actividad/biblioteca, red real, `BG00G`, builds ni smokes desktop.
- 2026-05-07: Quick win promo activa / contexto en `Último reporte` cerrado. La Web UI principal ahora muestra `active_promo_context` desde el último JSON local dentro de `Acciones y recomendaciones del último reporte`, con promo principal, categorías (`Fest`, `Weeklong`, etc.) y hasta 3 promos adicionales; el copy aclara que es contexto local y no predicción ni cambio de score. Evidencia: `.venv/bin/python -m unittest tests.test_web_assets` (31 OK, warning benigno conocido de Steam 403/fallback público) y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: mejora lectura contextual de promos sin tocar red real, reportes generados, cache policy, picks/cache por promo, `BG00G`, builds ni recalibración de scoring; el track sigue abierto para naming/picks/cache por promo.
- 2026-05-07: Quick win P1 UX / CTA principal antes de secciones secundarias cerrado. Se reordenó la Web UI compartida para que, tras la configuración de Steam Deals/PAYDAY 2, el bloque de utilidades, ayuda, `Generar reportes`/`Ejecutar PAYDAY 2`, detener y salida de ejecución aparezca antes de `Alertas de precio` e `Histórico de ejecuciones`; las secciones secundarias de Deals quedaron en `panel-deals-secondary` y se ocultan al cambiar a PAYDAY 2 para preservar sus controles. Evidencia: `.venv/bin/python -m unittest tests.test_web_assets` (30 OK, warning benigno conocido de Steam 403/fallback público), `.venv/bin/python -m unittest tests.test_track_history_flow` (14 OK) y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: baja la competencia visual antes del run principal sin tocar recomendaciones personalizadas, metadata de actividad/biblioteca, generator, red real, `BG00G`, builds ni reportes generados; el track de jerarquía visual sigue pendiente de revisión visual/manual browser/desktop.
- 2026-05-07: Auditoría Dashboard histórico / UX estática completada. Se revisó el panel `Histórico de ejecuciones` sin ejecutar browser ni abrir reportes: el bloque queda bajo `details`, conserva búsqueda por fecha/evento/perfil, filtros por estado, orden por delta, atajo `Comparar 2 recientes`, resumen visual por estado, cambios destacados, tendencia general no predictiva y drilldown `Historial por juego` con `Ver más juegos` para no saturar. Evidencia: revisión estática de `web/steam_deals/index.html`, `web/steam_deals/app.js`, `web/steam_deals/app.css`, `tests/test_web_assets.py` y `tests/test_track_history_flow.py`; checklist manual acotado agregado en `PENDIENTES.md`. Incidencias: no se cierra el track porque falta validación visual/manual real en browser. Impacto/decisión: el estado fuente es consistente con el MVP+ y queda listo el checklist final; no se validaron apertura/descarga de reportes generados, recomendaciones personalizadas, metadata de actividad/biblioteca, red real, `BG00G`, builds ni reportes generados.
- 2026-05-07: Track outputs/reportes / apertura y descarga de artefactos cerrado. Se validó que el flujo de reportes mantiene acciones y artefactos esperados para HTML interactivo, Share HTML, Markdown, JSON y CSV sin versionar outputs generados. Evidencia automática: `.venv/bin/python -m unittest tests.test_generated_files_serving tests.test_web_assets tests.test_desktop_share` (105 OK, warning benigno conocido de Steam 403/fallback público). Evidencia smoke real pequeño: `STEAM_DEALS_CACHE_DIR=/tmp/opencode/report-open-download-cache-csv STEAM_DEALS_LOG_DIR=/tmp/opencode/report-open-download-logs-csv ./.venv/bin/python steam_deals_generator.py --vanity "https://steamcommunity.com/id/joseluis12351" --output "/tmp/opencode/report-open-download-smoke-csv" --discount 0 --top 5 --csv` terminó OK en 34.3s y generó `.md`, HTML interactivo, Share HTML, `.json` y `.csv` en `/tmp/opencode/report-open-download-smoke-csv/`. Incidencias: biblioteca propia rechazó API key con HTTP 401 y continuó sin datos de biblioteca; Steam degradó batches de precios con HTTP 400 pero resolvió por fallback individual; primer smoke sin `--csv` generó los 4 artefactos no CSV y se repitió con flag aprobado. Impacto/decisión: se cierra el track P0 de apertura/descarga con evidencia real acotada, sin `BG00G`, cold-cache largo, build desktop, artefactos versionados ni cambios en recomendaciones personalizadas/metadatos de actividad/biblioteca.
- 2026-05-07: Auditoría UX principal / escaneabilidad estática completada. Se revisó fuente Web UI para confirmar jerarquía sin ejecutar browser/desktop: métricas principales del último reporte quedan acotadas a cuatro tarjetas, acciones/recomendaciones del último reporte van bajo `details`, la guía de métricas y ayuda de botones utilitarios son opcionales, el histórico permanece bajo demanda y `Generar reportes` queda separado de utilidades. Evidencia: revisión estática de `web/steam_deals/index.html`, `web/steam_deals/app.js`, `web/steam_deals/app.css` y `tests/test_web_assets.py`; `git diff --check` OK. Incidencias: no se cierra el track porque falta evidencia visual/manual real en browser/desktop. Impacto/decisión: el estado fuente es consistente con los quick wins cerrados; se dejó checklist manual acotado en `PENDIENTES.md` y el cierre final queda bloqueado por revisión visual/manual, sin validar apertura/descarga de reportes generados, sin recomendaciones personalizadas/metadatos de actividad/biblioteca, sin red real, `BG00G`, builds ni reportes generados.
- 2026-05-07: Quick win colecciones inteligentes / `Favoritos de comunidad` cerrado. `build_recommended_collections` agrega una colección `community_favorites` solo cuando combina adopción/popularidad cacheada (`review.total`, owners/ccu/players recientes de SteamSpy en `tags_data.players`) con calidad mínima (`review_pct`, score o Metacritic), con `source_signals` específicos y razón visible de popularidad + calidad; se omite si falta cualquiera de las dos señales para no duplicar `Aclamados` por pura calidad. Evidencia: `.venv/bin/python -m py_compile app/steam_deals_recommendations.py steam_deals_generator.py tests/test_generator_logic.py` OK, `.venv/bin/python -m unittest tests.test_generator_logic.RecommendedCollectionsTests` (10 OK), `.venv/bin/python -m unittest tests.test_generator_logic` (227 OK) y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: quedan cerradas las secciones ricas confiables actuales (`Story Rich`, `Singleplayer`, `Favoritos de comunidad`) usando solo datos existentes, sin Fanatical API, multi-store, red real, `BG00G`, reportes generados, builds ni recalibración del score base; futuras colecciones quedan pausadas hasta tener señales personalizadas adicionales.
- 2026-05-07: Quick win colecciones inteligentes / `Singleplayer` cerrado. `build_recommended_collections` agrega una colección `singleplayer` solo cuando existe señal explícita de categoría Steam `2` o tag/género `Singleplayer`/`Single-player`, con definición, `source_signals` específicos y razón visible; se omite si no hay señal, si todos los candidatos comparten la señal o si duplicaría `Por género/estilo`. Evidencia: `.venv/bin/python -m py_compile app/steam_deals_recommendations.py tests/test_generator_logic.py` OK, `.venv/bin/python -m unittest tests.test_generator_logic.RecommendedCollectionsTests` (8 OK), `.venv/bin/python -m unittest tests.test_generator_logic` (223 OK) y `git diff --check` OK. Incidencias: la primera validación mostró que `Singleplayer`/`Single-player` no se agrupaban igual que el criterio de omisión; con fix-forward aprobado se canonicalizó esa familia de términos para conteo y matching de estilo. Impacto/decisión: el track de colecciones gana otra sección confiable basada en señales locales sin Fanatical API, multi-store, red real, `BG00G`, reportes generados, builds ni recalibración del score base; `Favoritos de comunidad` queda como único candidato pendiente de sección rica si hay señal confiable.
- 2026-05-07: Quick win Desktop Doctor / claridad source-vs-frozen cerrado. El reporte del Doctor ahora muestra una línea `Modo: source` o `Modo: frozen` para explicar si los checks son preparación de dependencias/build o validación del runtime empaquetado y persistencia de datos; se cubrió con tests para ambos modos sin rebuild ni host nativo. Evidencia: `.venv/bin/python -m py_compile desktop_doctor.py tests/test_desktop_doctor.py` OK, `.venv/bin/python -m unittest tests.test_desktop_doctor` (6 OK) y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: reduce falsos cierres/confusión de readiness desktop sin tocar build, Windows/macOS, renderers, Shuffle, red real, `BG00G` ni reportes generados.
- 2026-05-07: Track `Shuffle 1 juego` / implementación de criterios cerrado. El HTML interactivo ahora arma candidatos en orden `personalized_recommendations` válidas → Top Picks → deals determinísticos, muestra score personal o score Top Pick con razones visibles, mantiene reroll con candidatos serializados y usa copy de candidato único cuando no hay alternativa; además restringe candidatos/enlaces/payloads de Shuffle a appids numéricos para evitar que datos maliciosos terminen en atributos interactivos. Evidencia: `.venv/bin/python -m py_compile renderers/html_renderer.py tests/test_generator_logic.py` OK, targeted personalizado/ranking/Shuffle (8 OK), `.venv/bin/python -m unittest tests.test_generator_logic` (221 OK) y `git diff --check` OK. Incidencias: la validación detectó sucesivamente fixtures incompletas, fallback personalizado por defecto, payload Shuffle con appid malicioso y cambios accidentales de fixtures no relacionadas; todos los fix-forward fueron acotados y aprobados antes de revalidar. Impacto/decisión: el track queda cerrado en renderer/fallback sin Web UI principal, selección simulada, red real, `BG00G`, reportes generados, build desktop ni recalibración global de scoring.
- 2026-05-07: Auditoría pendientes / criterios `Singleplayer` y `Favoritos de comunidad` aclarados. Se refinó en `PENDIENTES.md` que `Singleplayer` requiere señal explícita de categoría/tag single-player, candidatos distintos al bloque `Por género/estilo` y omisión cuando todos comparten esa señal; `Favoritos de comunidad` requiere popularidad/adopción verificable ya cacheada + calidad mínima, pool determinístico y razón visible de popularidad/calidad. Evidencia: docs-only y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: quedan criterios más estrictos para futuras colecciones sin implementar código, tocar renderers, usar red real, `BG00G`, reportes generados ni recalibrar score base.
- 2026-05-07: Evidencia visual local `Evalúa mi selección` registrada. Se validó con fixture local temporal y screenshot headless que el panel permanece dentro de `Acciones y recomendaciones del último reporte`, permite candidatos marcados + URL pegada, y muestra `Conservar`, `Dudar` y `Quitar` con razones compactas sin lenguaje de carrito/checkout. Evidencia: fixture `/tmp/opencode/selection-review-evidence/selection-review-visual.html`, screenshot `/tmp/opencode/selection-review-evidence/screenshots/selection-review-visual.png`, decisiones `['conservar', 'dudar', 'quitar']`. Incidencias: ninguna. Impacto/decisión: la UI se ve consistente en smoke local, pero la prueba visual/manual Windows sigue pendiente; no hubo red real, `BG00G`, reportes generados, builds ni cambios de implementación.
- 2026-05-07: Auditoría pendientes / criterios de `Shuffle 1 juego` cerrados. Se fijó en `PENDIENTES.md` que el siguiente slice real debe priorizar `personalized_recommendations` cuando existan, luego Top Picks y luego deals determinísticos por score/descuento; el reroll debe evitar repetir el juego inmediato, usar presupuesto/actividad solo si ya están en el reporte y mantener copy ligero sin prometer compra óptima. Evidencia: revisión documental y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: queda listo un slice de implementación posterior, sin tocar renderers ahora y sin mezclar Web UI principal, selección simulada, red real, `BG00G`, reportes generados ni recalibración de scoring.
- 2026-05-07: Auditoría pendientes / criterios de `trend` cerrados. Se fijó en `PENDIENTES.md` que `trend` se conserva solo como señal secundaria de `Historial local`: mostrar con historial suficiente y señal accionable (`Mín. local`, primera vez a este precio o HTML con 2+ snapshots), mantener copy no predictivo, ocultar ruido genérico/primeras apariciones y reemplazar solo por mínimo/histórico local o acceso a historial si aporta más claridad. Evidencia: revisión documental de estado actual HTML/Markdown/tests y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: no se tocan renderers ni se mezcla con Dashboard histórico, promo picks, recomendaciones personalizadas, datos externos, red real, `BG00G` o reportes generados.
- 2026-05-07: Quick win colecciones inteligentes / `Story Rich` cerrado. `build_recommended_collections` agrega una colección `story_rich` solo cuando existe señal explícita `Story Rich` en `tags`/`genres`, con definición, `source_signals` específicos y razón visible; el bloque se omite si no hay señal o si duplicaría `Por género/estilo`. Evidencia: `.venv/bin/python -m py_compile app/steam_deals_recommendations.py tests/test_generator_logic.py` OK, `.venv/bin/python -m unittest tests.test_generator_logic.RecommendedCollectionsTests` (6 OK), `.venv/bin/python -m unittest tests.test_generator_logic` (219 OK) y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: se retoma el track de colecciones ricas con una señal local confiable sin Fanatical API, multi-store, red real, `BG00G`, reportes generados, builds ni recalibración del score base; `Singleplayer`/`Favoritos de comunidad` siguen pendientes solo si hay señales explícitas suficientes.
- 2026-05-07: Validación local `Evalúa mi selección` / checklist mínimo cerrado. Se validó de forma local y acotada que el simulador vive dentro del desplegable `Acciones y recomendaciones del último reporte`, expone candidatos marcables, acepta pegado de AppIDs/URLs, contempla estados `Conservar`/`Dudar`/`Quitar`, maneja vacío/entrada inválida y mantiene copy explícito de que no abre carrito ni compra nada. Evidencia: `.venv/bin/python -m unittest tests.test_generated_files_serving tests.test_web_assets` (63 OK, warning benigno Steam 403 conocido), checklist estático de marcadores UI OK y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: el checklist local mínimo queda cerrado; solo quedan refinamientos si una revisión visual real muestra fricción, sin red real, `BG00G`, reportes generados, build desktop, checkout/carrito real ni recalibración global.
- 2026-05-07: Auditoría UX `Evalúa mi selección` / checklist visual-manual definido. Se dejó en `PENDIENTES.md` un checklist acotado para revisar el simulador local dentro del desplegable del último reporte: visibilidad sin competir con métricas, marcado de candidatos, pegado de AppIDs/URLs, resultados `Conservar`/`Dudar`/`Quitar` con razones, manejo de entradas vacías/invalidas y ausencia de lenguaje de compra/carrito/checkout. Evidencia: actualización docs-only y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: la próxima revisión manual queda enfocada sin abrir red real, `BG00G`, reportes generados, build desktop, pagos, tiendas externas ni recalibración global.
- 2026-05-07: Quick win `Evalúa mi selección` / Web UI local cerrado. Se agregó `/api/selection-review` como endpoint local protegido por token, que carga el último JSON permitido del output, sanitiza/limita AppIDs o URLs pegadas y reutiliza `build_selection_review` sin ejecutar generator ni tocar red; la Web UI muestra un panel compacto dentro de `Acciones y recomendaciones del último reporte` para marcar candidatos del ranking/top picks o pegar una lista y ver `conservar`, `dudar` o `quitar` con señales compactas y razones. Evidencia: `.venv/bin/python -m py_compile app/steam_deals_recommendations.py steam_deals_generator.py steam_deals_web.py tests/test_generator_logic.py tests/test_generated_files_serving.py tests/test_web_assets.py` OK, `.venv/bin/python -m unittest tests.test_generator_logic tests.test_generated_files_serving tests.test_web_assets` (281 OK, warning benigno Steam 403 conocido) y `git diff --check` OK. Incidencias: primera validación detectó un assert de copy antiguo (`HTML, Share, JSON, carpeta y destacados`); con aprobación se actualizó a `HTML, Share, JSON, carpeta, selección y destacados` y pasó. Impacto/decisión: queda usable el primer simulador local sin checkout/carrito real, pagos, tiendas externas, reportes generados, red real, `BG00G`, builds ni recalibración global de scoring; queda revisión visual/manual si se quiere pulir UX.
- 2026-05-07: Quick win `Evalúa mi selección` / helper puro inicial cerrado. Se agregó `build_selection_review` como simulador local sin carrito real: recibe una selección tentativa, cruza deals/top picks, `personalized_recommendations`, señales opcionales de actividad/biblioteca, owned/family y score, preserva orden determinístico con dedupe de appids repetidos y devuelve `conservar`, `dudar` o `quitar` con `signals` y 1-2 razones. Evidencia: `.venv/bin/python -m py_compile app/steam_deals_recommendations.py steam_deals_generator.py tests/test_generator_logic.py` OK, `.venv/bin/python -m unittest tests.test_generator_logic.SelectionReviewTests` (5 OK), `.venv/bin/python -m unittest tests.test_generator_logic` (218 OK) y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: queda lista la base pura para una futura UI/lista local de selección; siguen fuera checkout, carrito real, pagos, tienda externa, reportes generados, red real, `BG00G`, builds y recalibración global de scoring.
- 2026-05-07: Quick win recomendaciones personalizadas / Web UI principal cerrado. La tarjeta `Resumen de tu última ejecución` ahora muestra `Recomendaciones personalizadas` dentro del desplegable `Acciones y recomendaciones del último reporte`, con máximo 3 items visibles, razones compactas, perfil resumido, links Steam seguros para appids numéricos y enlaces al HTML/JSON completo; si el JSON no trae items, la sección se oculta. Evidencia: `.venv/bin/python -m py_compile tests/test_web_assets.py` OK, `.venv/bin/python -m unittest tests.test_web_assets` (28 OK, warning benigno Steam 403 conocido), `.venv/bin/python -m unittest tests.test_web_assets tests.test_generator_logic` (241 OK) y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: el ranking personalizado ya está en la Web UI sin competir con métricas ni crear tarjeta permanente; siguen fuera simulador de selección, colecciones ricas, red real, `BG00G`, builds y recalibración de scoring.
- 2026-05-07: Quick win recomendaciones personalizadas / Share HTML cerrado. El Share HTML ahora recibe o calcula `personalized_recommendations` y renderiza una sección compacta `Recomendaciones personalizadas` con perfil usado, score personal, afinidad, precio/descuento y razones visibles; appids numéricos conservan link Steam/botón Share y appids no numéricos no generan links ni payloads. Evidencia: `.venv/bin/python -m py_compile renderers/share_html_renderer.py steam_deals_generator.py` OK, tests Share HTML dirigidos (3 OK), `.venv/bin/python -m unittest tests.test_generator_logic` (213 OK) y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: el ranking personalizado ya está visible en JSON, Markdown, HTML generado y Share HTML; el siguiente slice natural es Web UI principal dentro del desplegable del último reporte, sin red real, sin `BG00G`, sin cold-cache largo, sin Fanatical API ni recalibrar score global.
- 2026-05-07: Quick win bloqueante recomendaciones personalizadas / criterio Web UI cerrado. Se definió que la Web UI principal solo debe mostrar `personalized_recommendations` dentro del desplegable `Acciones y recomendaciones del último reporte`, con máximo 3 items visibles, razones compactas, enlace al HTML/JSON para detalle y ocultamiento si no hay items. Evidencia: actualización docs-only en `PENDIENTES.md` y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: queda desbloqueado el slice Web UI sin competir con métricas ni crear otra tarjeta permanente; siguen fuera Share HTML, simulador de selección, colecciones ricas, red real, `BG00G` y recalibración de scoring.
- 2026-05-07: Quick win recomendaciones personalizadas / HTML generado cerrado. El HTML interactivo generado ahora renderiza `Recomendaciones personalizadas` desde `personalized_recommendations`, con tarjetas, cápsula Steam solo para appids numéricos, perfil usado, score personal, afinidad, precio/descuento y razones visibles; el caso vacío omite la sección. Evidencia: `.venv/bin/python -m py_compile renderers/html_renderer.py steam_deals_generator.py tests/test_generator_logic.py` OK, `.venv/bin/python -m unittest tests.test_generator_logic.RankTopPicksTests` (41 OK), `.venv/bin/python -m unittest tests.test_generator_logic` (210 OK) y `git diff --check` OK. Incidencias: el primer test de escaping detectó que un appid malicioso podía quedar como texto escapado en un atributo; con aprobación se hizo fix-forward para usar appids solo si son numéricos y omitir score/afinidad no numéricos. Impacto/decisión: el ranking personalizado ya es visible en HTML generado sin tocar Web UI principal ni Share HTML, sin red real, sin `BG00G`, sin cold-cache largo, sin Fanatical API ni recalibrar score global.
- 2026-05-07: Auditoría pendientes / criterios de `Shuffle 1 juego` acotados. Se aclaró en `PENDIENTES.md` que el siguiente ajuste del Shuffle debe decidir criterios antes de tocar implementación: priorizar Top Picks, evitar repetir el mismo juego inmediato, usar presupuesto/actividad solo si ya están disponibles en el reporte y mantener fallback determinístico. Evidencia: revisión documental y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: se evita mezclar este pendiente con Web UI principal, selección simulada, red real, `BG00G`, reportes generados o recalibración de scoring.
- 2026-05-07: Quick win recomendaciones / cápsula en colecciones HTML cerrado. El HTML interactivo generado ahora añade una mini cápsula clicable al primer juego visible de cada tarjeta `Colecciones recomendadas` cuando el `appid` es numérico, usando la URL CDN Steam ya derivable del `appid` y `loading="lazy"`; los appids no numéricos no renderizan imagen/link extra y el contrato `recommended_collections` no cambia. Evidencia: `.venv/bin/python -m py_compile renderers/html_renderer.py tests/test_generator_logic.py` OK, `.venv/bin/python -m unittest tests.test_generator_logic` (207 OK) y `git diff --check` OK. Incidencias: dos fix-forward aprobados ajustaron el render real y el test de dedupe para aceptar que imagen y nombre apunten al mismo juego sin duplicar items. Impacto/decisión: mejora la escaneabilidad del HTML generado sin reabrir colecciones ricas, scoring, Fanatical API, multi-store, recomendaciones personalizadas, red real, `BG00G`, build desktop ni reportes generados; queda revisión visual manual cuando se retome el smoke.
- 2026-05-07: Auditoría pendientes / decisión sobre `trend` acotada. Se aclaró en `PENDIENTES.md` que el track de tendencia debe decidir entre conservarla como señal secundaria, ocultarla más agresivamente, fusionarla con `Historial local`/histórico o reemplazarla por algo más accionable; además se agregó Quick Win para fijar criterios antes de tocar renderers. Evidencia: revisión documental y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: se evita mezclar este pendiente con Dashboard histórico, picks por promo, recomendaciones personalizadas, nuevos datos externos, red real, `BG00G` o reportes generados.
- 2026-05-07: Quick win recomendaciones personalizadas / Markdown de ranking personalizado cerrado. El Markdown principal ahora renderiza `## 🎯 Recomendaciones personalizadas` cuando `personalized_recommendations` trae items, con explicación de alcance, perfil usado (actividad, biblioteca, HLTB/precio si existen), `personalized_score`, `affinity_score`, precio/descuento y razones visibles por juego; si no hay items, omite la sección. Evidencia: `.venv/bin/python -m py_compile renderers/markdown_renderer.py steam_deals_generator.py tests/test_generator_logic.py` OK, `.venv/bin/python -m unittest tests.test_generator_logic` (207 OK) y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: el ranking personalizado ya es visible en Markdown sin tocar HTML/Web UI/Share, sin red real, sin `BG00G`, sin cold-cache largo, sin Fanatical API ni recalibrar score global; siguiente slice natural es HTML generado.
- 2026-05-07: Auditoría pendientes / cápsula visual en colecciones acotada. Se aclaró en `PENDIENTES.md` que la posible imagen/cápsula en `Colecciones recomendadas` es una decisión visual/readiness y solo debe usar imágenes ya disponibles en el payload/datos actuales; si requiere fetch nuevo, señal nueva, contrato, scoring o enriquecimiento queda fuera del slice. Evidencia: revisión documental y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: se evita que el pendiente derive en expansión de colecciones, red nueva, Fanatical API, multi-store o mezcla con recomendaciones personalizadas.
- 2026-05-07: Quick win recomendaciones personalizadas / JSON de ranking personalizado cerrado. `generate_json` ahora calcula y serializa `personalized_recommendations` como campo top-level, con `summary.personalized_recommendations_count`, items con `personalized_score`/`affinity_score`/razones visibles y perfil resumido; también preserva override explícito vacío para consumidores/tests. Evidencia: `.venv/bin/python -m py_compile app/steam_deals_recommendations.py renderers/json_renderer.py steam_deals_generator.py tests/test_generator_logic.py` OK, `.venv/bin/python -m unittest tests.test_generator_logic` (205 OK) y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: el ranking personalizado queda consumible por JSON para futuros renderers/UI sin HTML/Web UI/Share todavía, sin red real, sin `BG00G`, sin cold-cache largo, sin Fanatical API ni recalibrar score global.
- 2026-05-07: Quick win P1 UX `Resumen de tu última ejecución` compacto cerrado. La tarjeta del último reporte ahora muestra inicialmente solo las cuatro métricas principales (`Ofertas`, `Top picks`, `Alerts`, `Regalos`) y agrupa el siguiente paso, acciones HTML/Share/JSON/carpeta, colecciones recomendadas, compartir destacados y presupuesto en un bloque desplegable `Acciones y recomendaciones del último reporte`, conservando todos los accesos sin competir visualmente con las métricas. Evidencia: `.venv/bin/python -m py_compile tests/test_web_assets.py` OK, `.venv/bin/python -m unittest tests.test_web_assets` (27 OK, warning benigno Steam 403 conocido) y `git diff --check` OK. Incidencias: primer test de assets falló por buscar la llamada antigua a colecciones fuera del nuevo helper; fix-forward aprobado actualizó el marcador del test y pasó. Impacto/decisión: avanza la jerarquía visual reportada en Windows sin red real, `BG00G`, build desktop ni reportes generados; queda revisión visual manual cuando se retome el smoke.
- 2026-05-07: Auditoría pendientes / secuencia de recomendaciones personalizadas aclarada. Se agregó en `PENDIENTES.md` el orden recomendado de slices para evitar mezclar alcances: JSON/reporte sin UI, luego outputs generados, después Web UI si no compite visualmente, y finalmente `Evalúa mi selección` reutilizando el ranking expuesto; también se aclaró que esa selección puede funcionar como simulador/lista local, no como carrito real de tienda. Evidencia: actualización documental; validación docs-only con `git diff --check` OK. Incidencias: `git diff --check` detectó trailing whitespace preexistente en `web/steam_deals/app.css` y se limpió solo ese whitespace antes de revalidar. Impacto/decisión: el próximo implementador tiene una ruta incremental y no debe mezclar UI, colecciones ricas ni selección tentativa en el slice JSON/reporte.
- 2026-05-07: Quick win outputs/Web UI `Último reporte` 403 cerrado. El payload SSE `done.files` ahora publica solo nombres seguros de artefactos generados para construir enlaces `/files/...`, en vez de rutas absolutas redactadas que podían producir basenames inválidos y terminar en `Error 403 / Archivo no disponible` al pulsar `Abrir reporte interactivo`; las rutas no allowlisted siguen bloqueadas/redactadas y no se debilita la allowlist de `/files`. Evidencia: `.venv/bin/python -m py_compile steam_deals_web.py tests/test_generated_files_serving.py` OK, `.venv/bin/python -m unittest tests.test_generated_files_serving tests.test_web_assets` (59 OK, warning benigno Steam 403 conocido) y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: el flujo inmediato post-run y la tarjeta de último reporte generan links accionables sin exponer rutas locales; no hubo red real, `BG00G`, build desktop ni reportes generados.
- 2026-05-07: Auditoría pendientes / siguiente Quick Win de recomendaciones personalizadas registrado. Tras confirmar que el helper puro `build_personalized_recommendations` ya estaba cerrado, se agregó como pendiente explícito el siguiente slice: conectar el ranking personalizado al contrato JSON/reporte sin UI nueva, preservando compatibilidad de shape y razones visibles. Evidencia: actualización documental en `PENDIENTES.md`; validación docs-only con `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: el implementador de Quick Wins tiene un corte ejecutable y acotado sin reabrir UI, Fanatical API, multi-store, red real, `BG00G`, cold-cache ni recalibración global.
- 2026-05-07: Quick win recomendaciones personalizadas / helper puro inicial cerrado. `build_personalized_recommendations` arma un ranking determinístico desde datos ya disponibles de reporte (`deals`/`top_picks`) más señales opcionales de actividad, biblioteca, `owned`/family, `me gusta`, relaciones `similar a` y HLTB; devuelve `personalized_score`, `affinity_score`, razones visibles y resumen de perfil con términos de actividad, biblioteca, horas HLTB, precio promedio y exclusiones. Evidencia: `.venv/bin/python -m py_compile app/steam_deals_recommendations.py steam_deals_generator.py tests/test_generator_logic.py` OK, `.venv/bin/python -m unittest tests.test_generator_logic` (203 OK) y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: el track ya tiene una base pura/reutilizable para conectar después a JSON/reporte/UI sin Fanatical API, sin red real, sin `BG00G`, sin cold-cache largo ni recalibrar el score global.
- 2026-05-06: Hallazgos visuales Windows registrados en backlog. A partir de `/home/adolfo/Downloads/st1.jpeg`, `st2.jpeg` y `st3.jpeg` se agregaron Quick Wins para compactar `Resumen de tu última ejecución`, corregir el `Error 403 / Archivo no disponible` al abrir reporte interactivo desde `Último reporte`, y evaluar cápsula/imagen en `Colecciones recomendadas` sin reabrir expansión de colecciones ricas. Evidencia: revisión visual de screenshots locales y actualización de `PENDIENTES.md`; validación docs-only con `git diff --check`. Incidencias: ninguna en documentación. Impacto/decisión: quedan priorizados como pendientes trazables antes de nuevos cambios de recomendaciones personalizadas.
- 2026-05-06: Decisión de prioridad recomendaciones registrada. Se pausa seguir expandiendo `Colecciones inteligentes tipo “Recomendado para ti”` después de cerrar sus habilitadores básicos y se prioriza primero `Recomendaciones personalizadas por actividad y biblioteca`, porque las colecciones ricas necesitan señales más sólidas de actividad, biblioteca, géneros, horas y preferencias para ser útiles y no superficiales/repetitivas. Evidencia: docs-only en `PENDIENTES.md`. Incidencias: ninguna. Impacto/decisión: al cerrar el track personalizado, se podrá retomar colecciones con secciones más ricas y explicables reutilizando esas señales.
- 2026-05-06: Quick win recomendaciones / auditoría de señales extra cerrada. Se registraron criterios de entrada/omisión para no agregar todavía colecciones `Story Rich`, `Singleplayer` ni `Favoritos de comunidad` sin señal confiable y diferenciada: tag/estilo narrativo explícito, categoría/tag single-player explícito, o adopción/popularidad con calidad mínima respectivamente; si la señal solo duplica `Por género/estilo` o `Aclamados`, la colección se omite. Evidencia: revisión documental de `app/steam_deals_recommendations.py`, `app/steam_deals_prices.py`, `app/steam_deals_presentation.py`, `app/steam_deals_enrichment.py`, fixtures en `tests/test_generator_logic.py` y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: el track queda protegido contra secciones ruidosas; no se implementan nuevos IDs, Fanatical API, multi-store, red real, `BG00G`, builds ni reportes generados.
- 2026-05-06: Readiness recomendaciones / selección tentativa completada. Se detalló en `PENDIENTES.md` el inventario para “Evalúa mi selección” sin carrito real: señales existentes candidatas (tags/géneros, actividad, biblioteca/owned/family, wishlist/deals, score/reasons/top picks, precio/descuento/mínimo histórico y colecciones), primer slice recomendado como helper puro con fixtures y salida `conservar`/`dudar`/`quitar`, y no-hacer permanente de comercio. Evidencia: docs-only; `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: el futuro track queda preparado sin implementar UI, scoring, contrato, checkout, pagos, keys, tienda externa, red real, `BG00G`, builds ni reportes generados.
- 2026-05-06: Backlog recomendaciones / selección tentativa registrada. Se agregó como futuro track la idea “rate my cart” sin carrito real: ayudar a depurar una selección antes de comprar con señales existentes de gustos, actividad reciente, más jugados, géneros, biblioteca, wishlist, score, precio y solapamiento, devolviendo `conservar`/`dudar`/`quitar` con razones. Evidencia: actualización documental en `PENDIENTES.md`; validación docs-only con `git diff --check`. Incidencias: ninguna. Impacto/decisión: queda explícito que no se implementa checkout, carrito real, pagos, tienda externa, venta de keys ni remoción automática.
- 2026-05-06: Quick win recomendaciones / Web UI principal de colecciones cerrado. La tarjeta `Resumen de tu última ejecución` ahora renderiza una sección compacta `Colecciones recomendadas` desde `report.recommended_collections` servido por `/api/latest-report`, con hasta 4 colecciones, razones visibles, metadata score/descuento/precio, links Steam seguros para appids numéricos y dedupe visual entre tarjetas; se oculta si el JSON no trae colecciones. Evidencia: `.venv/bin/python -m unittest tests.test_web_assets` (27 OK), `.venv/bin/python -m unittest tests.test_web_assets tests.test_generator_logic` (227 OK) y `git diff --check` OK. Incidencias: warning benigno conocido de Steam 403 durante tests de assets. Impacto/decisión: las colecciones ya aparecen en la Web UI principal sin backend nuevo ni cambio de contrato; siguen fuera Fanatical API, multi-store, secciones adicionales, red real, `BG00G`, builds y reportes generados.
- 2026-05-06: Quick win recomendaciones / diversidad cross-collection cerrado. `build_recommended_collections` ahora prefiere appids no usados entre colecciones para reducir repetición también en wishlists grandes, manteniendo fallback repetido cuando el pool de candidatos no alcanza y sin cambiar IDs ni shape del contrato. Evidencia: `.venv/bin/python -m py_compile app/steam_deals_recommendations.py steam_deals_generator.py renderers/html_renderer.py tests/test_generator_logic.py` OK, `.venv/bin/python -m unittest tests.test_generator_logic` (200 OK) y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: la reducción de repetición ya no depende solo del render HTML; JSON/HTML/Markdown/Share reciben colecciones más diversas por defecto cuando hay alternativas. Sin Web UI principal, sin cambios al renderer Share HTML, sin Fanatical API, sin red real, sin `BG00G`, sin builds y sin reportes generados.
- 2026-05-06: Quick win recomendaciones / dedupe visual de colecciones HTML cerrado. El HTML interactivo ahora reduce repetición en `Colecciones recomendadas`: cada appid/nombre se muestra solo en la primera tarjeta visible y las tarjetas que quedarían únicamente con duplicados se omiten, con copy breve que aclara la decisión; no cambia el contrato `recommended_collections` ni afecta JSON, Markdown o Share HTML. Evidencia: `.venv/bin/python -m py_compile app/steam_deals_recommendations.py steam_deals_generator.py renderers/html_renderer.py tests/test_generator_logic.py` OK, `.venv/bin/python -m unittest tests.test_generator_logic` (200 OK) y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: el refinamiento observado en el smoke pequeño queda cerrado como Quick Win acotada; siguen fuera Web UI principal, Fanatical API, secciones adicionales, red real, `BG00G`, builds y reportes generados.
- 2026-05-06: Quick win recomendaciones / Share HTML de colecciones inteligentes cerrado. El Share HTML ahora recibe `recommended_collections` y renderiza una sección compacta `Colecciones recomendadas` con tarjetas por colección, razón por juego, metadata de score/descuento/precio y botón de compartir para appids numéricos; si la lista está vacía omite el bloque y appids no numéricos no generan link ni payload Share. Evidencia: `.venv/bin/python -m py_compile renderers/share_html_renderer.py steam_deals_generator.py tests/test_generator_logic.py` OK, tests Share HTML dirigidos (3 OK), `.venv/bin/python -m unittest tests.test_generator_logic` (197 OK) y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: el track de colecciones ya está disponible en contrato, JSON, HTML interactivo, Markdown principal y Share HTML; quedan fuera Web UI principal, refinamiento de repetición en wishlist pequeña y secciones adicionales. Sin Fanatical API, sin red real, sin `BG00G`, sin builds y sin reportes generados.
- 2026-05-06: Smoke visual recomendaciones / HTML generado completado. Se generó un reporte pequeño con `joseluis12351` para revisar el primer render de `Colecciones recomendadas` en el HTML interactivo y se capturó screenshot headless. Evidencia: `STEAM_DEALS_CACHE_DIR=/tmp/opencode/recommended-collections-cache STEAM_DEALS_LOG_DIR=/tmp/opencode/recommended-collections-logs .venv/bin/python steam_deals_generator.py --vanity "https://steamcommunity.com/id/joseluis12351" --output /tmp/opencode/recommended-collections-smoke --discount 0 --top 5` (9 juegos, 8 deals, OK ~35s), revisión estática HTML/JSON con 5 colecciones y screenshot `/home/adolfo/Pictures/Screenshots/recommended-collections-full.png`. Incidencias: intentos iniciales de screenshot fallaron por sesión gráfica/root y política `sudo`; se resolvió guardando la captura en la carpeta del usuario. Impacto/decisión: el bloque HTML es visible y usable, pero en wishlist pequeña repite varios juegos entre colecciones; se agrega follow-up como Quick Win de refinamiento sin cambiar contrato, Web UI principal, Share HTML, Fanatical API, red real, `BG00G` ni builds.
- 2026-05-06: Quick win recomendaciones / Markdown de colecciones inteligentes cerrado. El Markdown principal ahora recibe `recommended_collections` y renderiza `## 🧠 Colecciones recomendadas` con subsecciones por colección, descripción, juegos enlazados cuando hay appid, razón visible y metadata compacta de score/descuento/precio; si la lista está vacía omite el bloque. Evidencia: `.venv/bin/python -m py_compile renderers/markdown_renderer.py steam_deals_generator.py tests/test_generator_logic.py` OK, tests Markdown dirigidos OK, `.venv/bin/python -m unittest tests.test_generator_logic` (194 OK) y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: el track de colecciones ya está disponible en contrato, JSON, HTML interactivo y Markdown principal; quedan fuera Share HTML, Web UI principal, revisión visual/manual y secciones adicionales. Sin Fanatical API, sin red real, sin `BG00G`, sin builds y sin reportes generados.
- 2026-05-06: Quick win recomendaciones / HTML de colecciones inteligentes cerrado. El HTML interactivo generado ahora recibe `recommended_collections` y renderiza una sección `Colecciones recomendadas` con tarjetas por colección (`data-recommended-collection`), descripción, juegos, razón visible, score/descuento/precio cuando existen y omite la sección si la lista está vacía; appids no numéricos se muestran como texto escapado, no como links Steam. Evidencia: `.venv/bin/python -m py_compile app/steam_deals_recommendations.py steam_deals_generator.py renderers/html_renderer.py renderers/html_fallback_renderer.py tests/test_generator_logic.py` OK, `.venv/bin/python -m unittest tests.test_generator_logic` (192 OK) y `git diff --check` OK. Incidencias: la primera validación dirigida detectó que Top Picks asumía `discount`/`price_final`; fix-forward aprobado agregó fallbacks desde el deal fuente o valores vacíos/0. También se validó appid numérico para links Steam, descuentos malformados y escaping de datos de colección. Impacto/decisión: el track de colecciones ya tiene primer render en reporte HTML, pero sigue pendiente revisión visual/manual y no se integra aún a Web UI principal, Share HTML ni Markdown; sin Fanatical API, sin red real, sin `BG00G`, sin builds y sin reportes generados.
- 2026-05-06: Quick win recomendaciones / JSON de colecciones inteligentes cerrado. El flujo de generación calcula `recommended_collections` desde `deals` + `top_picks` usando el contrato puro existente y lo serializa en el JSON como campo top-level junto con `summary.recommended_collections_count`; `generate_json` también acepta una lista explícita para preservar control en tests/consumidores internos. Evidencia: `.venv/bin/python -m py_compile steam_deals_generator.py renderers/json_renderer.py tests/test_generator_logic.py` OK, `.venv/bin/python -m unittest tests.test_generator_logic` (189 OK) y `git diff --check` OK. Incidencias: comandos dirigidos iniciales usaron nombres de clase/método incorrectos; se repitieron con los nombres reales y pasaron. Impacto/decisión: el track de colecciones queda consumible desde JSON para un futuro render HTML/UI, sin Fanatical API, sin red real, sin `BG00G`, sin builds, sin reportes generados y sin recalibrar scoring base.
- 2026-05-06: Quick win recomendaciones / contrato de colecciones inteligentes cerrado. `app/steam_deals_recommendations.py` agrega `build_recommended_collections` como helper puro para generar colecciones determinísticas desde datos ya existentes del reporte: `recommended_for_you`, `best_savings`, `steam_deck`, `acclaimed` y `genre_style`, cada una con metadata (`id`, `title`/`label`, `description`, `source_signals`) e items con `appid`, nombre, razón y campos de precio/score disponibles. `steam_deals_generator.py` expone un wrapper compatible y `tests/test_generator_logic.py` cubre señales existentes, orden, límites, dedupe y caso vacío. Evidencia: `.venv/bin/python -m py_compile app/steam_deals_recommendations.py steam_deals_generator.py tests/test_generator_logic.py` OK, `.venv/bin/python -m unittest tests.test_generator_logic` (186 OK) y `git diff --check` OK. Incidencias: dos fix-forward aprobados para exponer el wrapper y alinear llamada keyword-only; luego se retiró el duplicado experimental y quedó un único contrato. Impacto/decisión: habilita el Track de colecciones tipo Fanatical sin UI todavía, sin Fanatical API, sin red real, sin `BG00G`, sin builds, sin reportes generados y sin recalibrar scoring base.
- 2026-05-06: Validación automatizada P1 UX de jerarquía visual completada. Se verificó por tests de assets que siguen presentes los marcadores de último reporte, guía de métricas, ayuda utilitaria colapsada, histórico bajo demanda, Share y guardias web relevantes. Evidencia: `.venv/bin/python -m unittest tests.test_web_assets` (26 OK, warning benigno Steam 403 esperado) y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: el track P1 UX queda listo para revisión visual/manual final; no se cierra como completo porque no se abrió browser/desktop real desde esta sesión.
- 2026-05-06: Quick win P1 UX `Histórico de ejecuciones` compacto cerrado. El bloque principal de histórico mantiene `history-card` y todos los controles/IDs usados por JS, pero queda dentro de un panel bajo demanda `Comparar ejecuciones anteriores`, con copy breve que lo marca como opcional para no competir visualmente con `Generar reportes`. Evidencia: `.venv/bin/python -m py_compile tests/test_web_assets.py` OK, `.venv/bin/python -m unittest tests.test_web_assets` OK y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: P1 UX avanza con otro corte acotado sin tocar backend, sin red real, sin `BG00G`, sin builds y sin reportes generados; el track completo sigue pendiente de revisión visual/manual final.
- 2026-05-06: Auditoría producto/recomendaciones inspirada en Fanatical registrada. Se revisaron las referencias locales `/home/adolfo/Pictures/Screenshots/eje1.png` a `eje4.png`, que muestran secciones curadas como `Recommended For You`, `Steam Deck Verified`, `Fan Favourites`, `Critically Acclaimed`, `Best Saving`, `Action`, `Singleplayer` y `Story Rich`. Decisión: tratarlo como Track de largo plazo (`Colecciones inteligentes tipo “Recomendado para ti”`) y agregar una Quick Win habilitadora conectada al track para definir contrato/señales iniciales con datos ya existentes del reporte; las Quick Wins de este frente deben facilitar el Track, no convertirse en planificación paralela. Evidencia: revisión documental + screenshots locales + `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: queda en backlog sin implementar código, sin integrar Fanatical API, sin red real, sin `BG00G`, sin builds y sin recalibrar scoring base.
- 2026-05-06: Revisión estática P1 UX de cierre parcial completada. Se revisó la estructura principal de `web/steam_deals/index.html` y estilos relacionados tras los slices de `Último reporte`, guía de métricas y ayuda utilitaria colapsada: la acción primaria `Generar reportes` queda separada de utilidades, las ayudas largas están bajo demanda y las opciones avanzadas siguen colapsadas. No se cierra el track porque no hubo validación visual/manual real en browser/desktop; se deja como pendiente explícito en `PENDIENTES.md` en vez de sobredeclarar cierre. Evidencia: revisión estática + `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: siguiente paso para P1 UX es validación visual/manual final o abrir otro slice solo si aparece fricción concreta.
- 2026-05-06: Quick win P1 UX ayuda utilitaria colapsada cerrado. La Web UI mantiene los botones `Probar config`, Doctor desktop, Autofix, Limpiar cache, Abrir último reporte y Carpeta de reportes, pero mueve sus explicaciones largas a un panel opcional `¿Qué hace cada botón utilitario?` para que el foco inicial quede en `Generar reportes` y no en tarjetas de ayuda permanentes. Evidencia: `.venv/bin/python -m unittest tests.test_web_assets` (26 OK, warning benigno Steam 403 esperado) y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: P1 UX avanza con un ajuste acotado sin rediseñar la UI, sin tocar endpoints, PAYDAY 2, builds, red real, `BG00G` ni reportes generados; queda pendiente revisión visual/manual final antes de cerrar el track completo.
- 2026-05-06: Quick win P1 UX guía de métricas compacta cerrado. La Web UI convirtió `Cómo leer las métricas principales` en una ayuda opcional colapsada (`details.metrics-guide`) que conserva `Score`, `Mínimo histórico`, `Antigüedad` y `Tendencia general`, reduciendo ruido inicial antes de ejecutar; como ajuste complementario de escaneabilidad, el resumen del último reporte separa `Siguiente mejor paso` de `Opciones técnicas del último reporte`. Evidencia: `.venv/bin/python -m py_compile tests/test_web_assets.py` OK, `.venv/bin/python -m unittest tests.test_web_assets tests.test_generated_files_serving` OK y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: avance parcial del track P1 de jerarquía visual sin tocar backend pesado, sin red real, sin `BG00G`, sin builds, sin reportes generados y sin rediseñar la app completa; queda revisión visual/manual final antes de cerrar el track completo.
- 2026-05-06: Quick win P1 UX `Último reporte` y acciones principales cerrado. La Web UI cambió el botón utilitario a `Abrir último reporte`, conserva carpeta como `Carpeta de reportes` y agrega dentro del resumen dinámico una franja `Acciones del último reporte` con HTML interactivo como acción primaria, Share HTML, JSON técnico, copiar URL JSON y carpeta de reportes; el copy separa el flujo recomendado de descargas técnicas para bajar ruido. Evidencia: `.venv/bin/python -m py_compile tests/test_web_assets.py` OK, `.venv/bin/python -m unittest tests.test_web_assets` OK y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: avance parcial del track P1 de jerarquía visual sin tocar backend pesado, sin red real, sin `BG00G`, sin builds y sin rediseñar la app completa; quedan hints/controles generales para futuros slices si hay fricción real.
- 2026-05-06: Quick win Wave 5 P3 frontera PAYDAY 2 standalone mediante registry cerrado. Se agregó `shared/tool_modules.py` con metadata inmutable de PAYDAY 2 (`id`, `name`, `description`, `entrypoint=payday2_dlc_tracker.py`, `default_port=8081`, namespaces `payday2`/`web/payday2` y `nav_mode=standalone_linked`) y helpers puros de lookup/listado; `shared/__init__.py` expone el contrato. `steam_deals_web.py` deriva `PD2_ENTRYPOINT` y `PD2_SCRIPT_PATH` desde el registry para source/frozen, preservando `/api/run-pd2`, `--run-script`, output, filtros y secrets en env. `tests/test_tool_modules.py` cubre metadata, lookup, comandos source/frozen, secrets fuera de argv y guardrail AST que falla si Steam Deals Web importa `payday2_web` o `payday2_dlc_tracker`. Evidencia: `.venv/bin/python -m py_compile shared/tool_modules.py steam_deals_web.py tests/test_tool_modules.py tests/test_web_assets.py` OK, `.venv/bin/python -m unittest tests.test_tool_modules tests.test_web_assets` (33 OK, warning benigno Steam 403 esperado) y `git diff --check` OK. Incidencias: el primer intento de subtask 03 detectó `tests/test_tool_modules.py`/`shared/tool_modules.py` ausentes en disco; con aprobación se hizo fix-forward restaurando los deliverables y la validación pasó. Impacto/decisión: cuarto slice Wave 5 P3 y track de drift arquitectónico quedan cerrados sin rediseñar UX, sin mover handlers PAYDAY 2, sin builds, sin red real, sin `BG00G` y sin reportes generados; siguiente seguimiento solo si aparece fricción real/regresión o host desktop nativo.
- 2026-05-06: Quick win Wave 5 P3 contrato Share/Compartir centralizado cerrado. Se agregó `shared/share_payload.py` como helper stdlib para `normalize_share_payload`, `encode_share_payload`, `decode_share_payload` y `build_steamtools_share_url`, dejando `share_payload.py` como wrapper compatible; el payload usa `v=1`, `steam_url` normalizado desde `appid`, aliases legacy (`original_price`/`price_original`, `historical_low`/`min_hist`, `steam_appid`/`appid`) y errores seguros. `steam_tools_desktop.py` delega el decode al helper compartido; los renderers HTML/Share construyen `data-share-game` con el contrato canónico; Web UI conserva markers equivalentes y el URL `steamtools://share?data=...` sin rediseñar el modal ni cambiar labels. Evidencia: `.venv/bin/python -m unittest tests.test_generator_logic tests.test_web_assets tests.test_desktop_share` (249 OK, warning benigno Steam 403 esperado), `.venv/bin/python -m py_compile share_payload.py shared/share_payload.py shared/__init__.py renderers/html_renderer.py renderers/share_html_renderer.py steam_tools_desktop.py tests/test_desktop_share.py tests/test_generator_logic.py tests/test_web_assets.py` OK y `git diff --check` OK. Incidencias: se separaron cambios prematuros del siguiente slice PAYDAY 2 registry antes de cerrar. Impacto/decisión: tercer slice Wave 5 P3 queda cerrado sin tocar PAYDAY 2, builds, red real, `BG00G` ni reportes generados; siguiente seguimiento natural es definir frontera PAYDAY 2 standalone mediante registry de módulos.
- 2026-05-06: Quick win Wave 5 P3 Fallbacks HTML/Share a renderers cerrado. `steam_deals_generator.py` conserva las rutas primarias `_generate_html_renderer`/`_generate_share_html_renderer`, pero cuando no están disponibles delega a `renderers/html_fallback_renderer.py` y `renderers/share_fallback_renderer.py`; se retiraron del generator los bloques grandes HTML/CSS/JS y la construcción inline de fallback HTML/Share, manteniendo IDs/classes/copy y payload Share sin rediseño ni centralización de contrato. Evidencia: `.venv/bin/python -m py_compile steam_deals_generator.py renderers/html_fallback_renderer.py renderers/share_fallback_renderer.py` OK, `.venv/bin/python -m unittest tests.test_generator_logic tests.test_web_assets tests.test_desktop_share` (241 OK, warning benigno Steam 403 esperado) y `git diff --check` OK. Incidencias: primer intento de script local usó `python` y falló porque no existe en PATH; con aprobación se reintentó usando `.venv/bin/python`. Impacto/decisión: segundo slice Wave 5 P3 queda cerrado sin tocar PAYDAY 2/Desktop/builds/reportes; siguiente seguimiento natural es centralizar contrato Share/Compartir.
- 2026-05-05: Quick win Wave 5 P3 Dashboard histórico a módulo puro cerrado. Se agregó `app/steam_deals_history_dashboard.py` para concentrar carga defensiva de runs, filename validation, summaries, comparación, filtros/sort, analytics y game history; `steam_deals_web.py` conserva solo imports + routing/query params para `/api/history/runs` y `/api/history/compare`, manteniendo monkeypatch/test compatibility y response shape actual. `tests/test_track_history_flow.py` ahora valida el módulo puro directamente y agrega cobertura para history dir faltante y JSON/payloads malformados. Evidencia: `.venv/bin/python -m py_compile steam_deals_web.py app/steam_deals_history_dashboard.py tests/test_track_history_flow.py` OK, `.venv/bin/python -m unittest tests.test_track_history_flow tests.test_web_assets` (40 OK) y `git diff --check` OK. Incidencias: ninguna. Impacto/decisión: primer slice Wave 5 P3 queda cerrado sin rediseño UI, sin mover assets, sin Share/PAYDAY 2 y sin cambiar contratos visibles; siguiente slice natural P3 es extraer fallbacks HTML/Share restantes hacia `renderers/`.
- 2026-05-05: Quick win Wave 3 PAYDAY 2 docs cache/force/diagnóstico cerrado. `payday2_guia.md` ahora explica flujo recomendado Web primero, diferencia entre refresh normal y **Forzar catálogo**/`--no-cache`, uso de `--diagnose-dlc`, archivos de cache (`dlc_list.json`, `dlc_mapping.json`, `prices.json`, `bundles.json`, `owned.json`, `price_history.json`), TTLs y estados de diagnóstico; `README.md` agrega ejemplos cortos y deriva el detalle a la guía; `docs/runbooks/features-validation.md` suma checklist PAYDAY 2 data/cache para validar futuros cambios sin red real. Evidencia: `git diff --check` OK y `.venv/bin/python -m unittest tests.test_payday2_diagnostics tests.test_web_assets tests.test_shared_web_infra tests.test_shared_cache_utils tests.test_runtime_paths` (75 OK). Incidencias: ninguna. Impacto/decisión: Wave 3 PAYDAY 2 cache/diagnóstico/docs queda cerrado sin hardcodear DLCs, sin prometer packages/bundles como DLCs del app base `218620` y sin tocar lógica.
- 2026-05-05: Quick win Wave 3 PAYDAY 2 diagnóstico DLC faltante cerrado. `payday2_dlc_tracker.py` agrega `--diagnose-dlc APPID_OR_NAME`, con helpers puros para parsear appid/nombre/bundle/sub, consultar `appdetails` del app base `218620` y del candidato, cruzar cache local (`dlc_list`, `dlc_mapping`, `prices`) y clasificar casos `listed_in_base_dlc`, `cache_stale`, `valid_app_not_linked_to_base`, `package_or_bundle_candidate`, `not_found_or_unreleased` o `name_mismatch`. La salida imprime evidencia mínima y acción sugerida sin hardcodear DLCs, sin agregar appids automáticamente y sin cambiar refresh normal ni ownership manual; `payday2_guia.md` documenta el uso básico. Evidencia: `.venv/bin/python -m py_compile payday2_dlc_tracker.py tests/test_payday2_diagnostics.py` OK, `.venv/bin/python -m unittest tests.test_payday2_diagnostics` (9 OK), `.venv/bin/python -m unittest tests.test_payday2_diagnostics tests.test_web_assets tests.test_shared_web_infra tests.test_shared_cache_utils tests.test_runtime_paths tests.test_host_loopback_payday2 tests.test_config_security` (109 OK) y `git diff --check` OK. Incidencias: la primera validación ampliada falló porque el fake handler de `tests/test_config_security.py` no tenía `_body()` tras el soporte previo de force refresh; con aprobación se ajustó el fake a body `{}` y la suite pasó. Impacto/decisión: Wave 3 queda con diagnóstico CLI testeado sin red real; siguiente seguimiento natural es cerrar documentación PAYDAY 2 sobre cache/force/diagnóstico o pasar a Wave 5 P3.
- 2026-05-05: Quick win Wave 3 PAYDAY 2 cache/fuente + force refresh cerrado. La Web UI ahora muestra estado compacto de fuente/cache para catálogo, nombres, precios y bundles, y agrega la acción secundaria `Forzar catálogo` que llama `/api/refresh` con `force=true`; el backend traduce esa acción a `payday2_dlc_tracker.py --no-cache` sin pasar secretos en `argv`, mientras `Actualizar datos` conserva el refresh normal con TTL/cache. El diagnóstico visible aclara que, si un DLC falta tras forzar, Steam puede no exponerlo como `data.dlc` del app base `218620` o publicarlo como package/bundle separado; no se hardcodearon DLCs ni se usó red real. Evidencia: `.venv/bin/python -m py_compile payday2_web.py payday2_dlc_tracker.py tests/test_web_assets.py` OK, `.venv/bin/python -m unittest tests.test_web_assets tests.test_shared_web_infra tests.test_shared_cache_utils tests.test_runtime_paths tests.test_host_loopback_payday2` (71 OK) y `git diff --check` OK. Incidencias: un test inicial esperaba copy literal en JS; con aprobación se corrigió para validar `status.diagnostic` en JS y el texto seguro en backend. Impacto/decisión: primer slice Wave 3 queda cerrado; siguiente seguimiento natural es diagnóstico PAYDAY 2 de DLC faltante por appid/nombre si el usuario lo prioriza.
- 2026-05-05: Limpieza docs-only de pendientes/auditoría. `PENDIENTES.md` dejó de apuntar al slice Wave 2 Performance ya cerrado como próximo paso operativo y quedó alineado con el estado actual: siguiente candidato PAYDAY 2 = diagnóstico de DLC faltante por appid/nombre, alternativa Wave 5 P3 = Dashboard histórico a módulo puro, y performance solo como benchmark/follow-up explícito. También se marcaron como superseded los checklists de kickoff Waves 1-6 porque el selector, los Ready Slices y los runbooks ya reemplazan esa meta-planificación. Evidencia esperada: revisión documental y `git diff --check`; no se ejecutaron tests runtime, builds, smokes, red real ni `BG00G`.
- 2026-05-05: Quick win Wave 2 Performance / presupuesto resumible de tiempo/candidatos cerrado. `app/steam_deals_prices.py` prioriza candidatos de refresh (`missing` → stale con deal viejo → stale sin deal → retry fuera de cooldown), permite limitar por `STEAM_DEALS_MAX_REFRESH_CANDIDATES_PER_RUN` y `STEAM_DEALS_PRICE_REFRESH_TIME_BUDGET_SECONDS`, guarda cache parcial al diferir y marca pendientes con `time_budget_deferred`, prioridad y `next_retry_after` sin perder cache vieja útil. `steam_deals_generator.py` expone tuning y métricas `processed_count`, `deferred_by_time_budget`, `time_budget_exhausted` y `next_resume_hint`; `app/steam_deals_warm_cache_summary.py` las parsea/formatea y el runbook warm-cache las agrega al checklist. Evidencia: `.venv/bin/python -m py_compile app/steam_deals_cache_policy.py app/steam_deals_prices.py app/steam_deals_warm_cache_summary.py steam_deals_generator.py` OK, `.venv/bin/python -m unittest tests.test_generator_logic tests.test_warm_cache_summary` (204 OK) y `git diff --check` OK. Impacto: tercer slice Wave 2 queda cerrado sin red real, sin `BG00G`, sin cold-cache largo, sin cambiar fallback budget ni TTL/jitter; siguiente performance debe ser medición explícita con cache/log aislado o follow-up basado en logs nuevos.
- 2026-05-05: Quick win Wave 6 next-action selector cerrado. `PENDIENTES.md` ahora concentra el selector de siguiente acción como fuente de verdad operativa, sin crear runbook paralelo: cada opción tiene primer slice ejecutable, blocker, validación mínima y siguiente acción para regresiones P0 seguridad, Wave 2 Performance, Wave 3 PAYDAY 2, P2 macOS, Wave 5 P3 y release/smoke. También se marcó cerrada la fase de planificación Wave 6 y `docs/project-rules.md` apunta al selector cuando existan varios frentes posibles. Evidencia: revisión documental y `git diff --check` OK; no se ejecutaron builds, smokes, red real, cache refreshes ni tests largos. Impacto: queda lista la transición de planificación a implementación, evitando nuevo meta-backlog salvo blocker real.
- 2026-05-05: Quick win Wave 6 stop-on-failure cerrado. Se agregó `docs/runbooks/stop-on-failure.md` con regla base para fallos de validación: detener, reportar comando/salida resumida/riesgo, proponer manejo, pedir aprobación antes de corregir o repetir comandos largos, y registrar solo la evidencia necesaria. Define decisiones `fix-forward`, `revert/pause`, `blocked por host`, `downgrade a backlog` y `alinear docs`; mapea stop conditions por docs-only, P0 seguridad, P0 performance, PAYDAY 2, P2 desktop, P3 arquitectura y outputs/share, y criterios para reanudar. `docs/runbooks/README.md`, `validation-matrix.md`, `smoke-test-catalog.md` y `docs/project-rules.md` lo enlazan. Evidencia: revisión documental y `git diff --check` OK; no se ejecutaron builds, smokes, red real, cache refreshes ni tests largos. Impacto: las waves no avanzan sobre base roja y `BG00G`/cold-cache no se ejecuta como reacción no planificada.
- 2026-05-05: Quick win Wave 6 smoke/test command catalog cerrado. Se agregó `docs/runbooks/smoke-test-catalog.md` con comandos mínimos copiables por área: docs/release hygiene, P0 seguridad base/Host, P0 performance parser/cache-policy, PAYDAY 2, P2 desktop, outputs/share y P3 arquitectura; también separa comandos opcionales para pytest selectivo, `py_compile`, rebuild desktop, fallback web dirigido, smoke pequeño con `joseluis12351` y benchmark `BG00G`. `docs/runbooks/README.md`, `docs/runbooks/validation-matrix.md` y `docs/project-rules.md` lo enlazan como catálogo complementario a la matriz. Evidencia: revisión documental y `git diff --check` OK; no se ejecutaron builds, smokes, red real, cache refreshes ni tests largos. Impacto: futuros slices pueden copiar el comando proporcional sin buscar en `BITACORA.md` ni convertir `BG00G`/builds en smokes rápidos.
- 2026-05-05: Quick win Wave 6 validation matrix cerrado. Se agregó `docs/runbooks/validation-matrix.md` para escoger validación proporcional por wave/slice sin lanzar smokes largos por defecto: docs-only/release hygiene, P0 seguridad, P0 performance determinística, PAYDAY 2, P2 desktop, P3 arquitectura, outputs/share y cross-platform manual quedan mapeados a validación mínima, smoke permitido, prerequisitos/blockers y evidencia esperada. `docs/runbooks/README.md`, `docs/runbooks/slice-readiness.md` y `docs/project-rules.md` enlazan la matriz; el catálogo de comandos concretos queda como slice separado. Evidencia: revisión documental y `git diff --check` OK; no se ejecutaron builds, smokes, red real, cache refreshes ni tests largos. Impacto: cada slice puede elegir QA proporcional y escalar solo si el cambio lo justifica.
- 2026-05-05: Quick win Wave 2 Performance / `stale-while-revalidate` con jitter determinístico cerrado. `app/steam_deals_cache_policy.py` agrega `stable_ttl_jitter_hours()`, clasifica cache scoped en `fresh`, `stale_usable` y `stale_expired`, aplica TTL efectivo `ENTRY_REFRESH_TTL_HOURS + jitter`, usa ventana conservadora de `stale_grace` y limita el refresh de stale no crítico por corrida. `steam_deals_generator.py` activa la política con `PRICE_TTL_JITTER_HOURS=6`, `PRICE_STALE_GRACE_HOURS=72` y `PRICE_MAX_STALE_REFRESH_PER_RUN=200`, mantiene `missing` como prioridad, conserva `--no-cache` como refresh completo y reporta `stale_used_count`, `stale_refresh_deferred_count` y buckets de jitter. `app/steam_deals_warm_cache_summary.py` parsea/formatea esas métricas y el runbook warm-cache las agrega al checklist. Evidencia: `.venv/bin/python -m py_compile app/steam_deals_cache_policy.py app/steam_deals_warm_cache_summary.py steam_deals_generator.py` OK, `.venv/bin/python -m unittest tests.test_generator_logic tests.test_warm_cache_summary` (200 OK) y `git diff --check` OK. Impacto: segundo slice Wave 2 queda cerrado sin red real, sin `BG00G`, sin cold-cache largo, sin tocar fallback budget y sin implementar todavía el presupuesto de tiempo/candidatos; siguiente slice natural: presupuesto resumible de tiempo/candidatos.
- 2026-05-05: Quick win Wave 6 risk register cerrado. `PENDIENTES.md` ahora contiene un registro compacto de riesgos/decisiones activas como fuente de verdad operativa, con impacto, probabilidad, track/slice, estado/decisión, mitigación, trigger de revisión y criterio de cierre. La tabla cubre macOS sin host nativo, `BG00G` cold-cache largo, hardening local, PAYDAY 2 DLCs no expuestos por Steam, constraints/native deps, source/frozen/Doctor, artefactos/logs/spec y drift P3; `docs/project-rules.md` y `docs/runbooks/README.md` aclaran cuándo actualizarlo. Evidencia: revisión documental y `git diff --check` OK; no se ejecutaron builds, smokes, cache refreshes ni tests largos. Impacto: los blockers/tradeoffs activos tienen mitigación y ruta de cierre sin crear un documento paralelo a `PENDIENTES.md`.
- 2026-05-05: Quick win Wave 6 slice readiness cerrado. Se agregó `docs/runbooks/slice-readiness.md` con mini-ficha previa por slice: objetivo, fuera de alcance, archivos probables, dependencias, riesgos/blockers, validación mínima, docs afectados, evidencia esperada, rollback/compatibilidad y no-hacer. Incluye variantes docs-only, P0 seguridad, P0 performance, PAYDAY 2, P2 desktop, P3 arquitectura y release hygiene, más ejemplo aplicado a PAYDAY 2 force refresh; `docs/runbooks/README.md` y `docs/project-rules.md` lo enlazan como paso proporcional antes de implementar. Evidencia: revisión documental y `git diff --check` OK; no se ejecutaron builds, smokes, cache refreshes ni tests largos. Impacto: futuros quick wins arrancan con alcance/validación/evidencia claros sin crear burocracia larga.
- 2026-05-05: Quick win Wave 6 docs alignment cerrado. Se agregó `docs/runbooks/docs-alignment.md` con matriz de source-of-truth: `PENDIENTES.md` para estado/prioridades/blockers, `BITACORA.md` para evidencia, `README.md` para uso público, runbooks para procedimientos, `docs/project-rules.md` para reglas del repo y contexto técnico para arquitectura/stack. También mapea temas activos a docs que deben actualizarse juntos y agrega checklist antes de cerrar slices. Evidencia: revisión documental y `git diff --check` OK; no se ejecutaron builds, smokes ni tests largos. Impacto: reduce drift documental y evita duplicar README/runbooks/backlog.
- 2026-05-05: Quick win Wave 6 runbook index cerrado. `docs/runbooks/README.md` ahora incluye un selector rápido por wave/tipo de slice con ruta, validación mínima y regla de no repetir para P0 seguridad, performance, PAYDAY 2, P2 desktop Linux/macOS/Windows, constraints, release hygiene/evidencia y P3 arquitectura. El índice mantiene `PENDIENTES.md` como source-of-truth de estado y enlaza runbooks sin duplicar procedimientos largos. Evidencia: revisión documental y `git diff --check` OK; no se ejecutaron builds, smokes ni tests largos. Impacto: un agente/contribuidor puede elegir el runbook correcto sin releer todo el backlog y los blockers `macOS host nativo`/`BG00G solo performance explícita` quedan visibles.
- 2026-05-05: Quick win Wave 6 evidence template cerrado. Se agregó `docs/runbooks/evidence-template.md` para estandarizar cierres compactos: `BITACORA.md` guarda objetivo, evidencia, artefactos, incidencias, impacto y seguimiento; `PENDIENTES.md` conserva una frase de cierre con impacto/validación/bloqueo. El formato cubre variantes docs-only, tests-only, desktop manual, performance y PAYDAY 2/data-cache, y refuerza no pegar logs/reportes completos, tracebacks largos ni secretos. Evidencia: revisión documental y `git diff --check` OK; no se ejecutaron builds, smokes ni tests largos. Impacto: futuros quick wins quedan trazables sin duplicar evidencia extensa.
- 2026-05-05: Quick win Wave 2 Performance / presupuesto adaptativo de fallback individual cerrado. `app/steam_deals_prices.py` mide `individual_attempts` y `individual_no_data`, activa presupuesto conservador tras mínimo de 80 muestras con ratio alto de no-data o límite absoluto, difiere solo candidatos no críticos (`stale_without_old_deal`/`failure_retry`) a cooldown con `fallback_budget_deferred`, conserva `missing` como crítico y preserva cache vieja útil cuando una revalidación individual no trae datos. `steam_deals_generator.py` expone las métricas `deferred_by_fallback_budget`, `fallback_budget_reason` y `old_cache_used_count`, y `app/steam_deals_warm_cache_summary.py` las parsea/formatea para evidencia offline. Evidencia: `.venv/bin/python -m py_compile app/steam_deals_prices.py app/steam_deals_warm_cache_summary.py steam_deals_generator.py` OK, `.venv/bin/python -m unittest tests.test_generator_logic tests.test_warm_cache_summary` (193 OK) y `git diff --check` OK. Impacto: el primer slice Wave 2 queda cerrado sin red real, sin `BG00G`, sin cold-cache largo, sin cambiar TTL general y sin mezclar `stale-while-revalidate`; siguiente slice natural: `stale-while-revalidate` con jitter determinístico.
- 2026-05-05: Quick win Wave 6 release hygiene / artefactos generados cerrado. Se agregó `docs/runbooks/release-hygiene.md` como política compacta de qué se versiona y qué queda local: código/assets/web/tests/docs/requirements/constraints/CI son source-of-truth; `output/`, `logs/`, `.cache/`, `build/`, `dist/`, reportes `Steam Deals*`, `PAYDAY2_Plan_de_Compra.*` y `*.spec` no se versionan. `.gitignore` ahora cubre `output/`; README, `docs/project-rules.md` y el índice de runbooks enlazan la política. Decisión explícita: `SteamToolsDesktop.spec` sigue como artefacto generado/local ignorado porque puede contener rutas absolutas; `build_desktop.py` es la fuente operativa salvo futuro slice de spec estable. Evidencia: revisión documental; no se borró evidencia local, no se ejecutaron builds/smokes ni se cambiaron rutas runtime. Impacto: los próximos smokes/builds tienen checklist de repo limpio y registro de evidencia sin commitear artefactos generados.
- 2026-05-05: Quick win P2 Linux/macOS checklist cerrado sin repetir evidencia larga. Se consolidó el criterio de cierre en `docs/runbooks/desktop-linux.md`, `docs/runbooks/desktop-macos.md`, `docs/runbooks/desktop-windows.md`, `docs/runbooks/README.md` y `PENDIENTES.md`: Linux conserva como evidencia válida el build/binario con ventana nativa, outputs `.md/.html/.csv/.json/share.html`, cierre limpio y slices posteriores de runtime frozen, Doctor, **Copiar log**, fallback web y constraints; no se debe repetir `BG00G` ni el E2E largo salvo cambio sustancial del binario/runtime, objetivo performance explícito o gate Ubuntu LTS exacto. macOS queda explícitamente bloqueado hasta validar `.app` en host nativo; CI/Windows/source/fallback no sustituyen ese cierre. Evidencia: revisión documental y actualización de checklists; no se ejecutaron rebuilds, smokes largos ni host nativo. Impacto: P2 queda con próximo paso claro y sin duplicar logs largos; falta validación macOS cuando exista host.
- 2026-05-05: Quick win P2 desktop/readiness constraints desktop/CI cerrado. Se separó intención de dependencias (`requirements-desktop.txt`) de resolución validada (`constraints/desktop.txt`) para evitar depender de `latest` implícito en PyInstaller/pywebview/Qt. El constraints compartido fija el set validado localmente (`pyinstaller==6.20.0`, `pywebview==6.2.1`, stack PyQt6/QtPy Linux, hooks y transitivas relevantes) sin introducir upgrades oportunistas. `build_desktop.py` y Desktop Doctor/autofix ahora instalan con `-c constraints/desktop.txt` cuando está disponible; CI desktop usa el mismo constraints; README y runbooks Linux/Windows/macOS actualizan comandos de instalación, y `docs/runbooks/desktop-constraints.md` documenta proceso de refresh controlado en `.venv` limpio, validaciones mínimas y límites por plataforma. Evidencia: `.venv/bin/python -m pip install --dry-run -r requirements-desktop.txt -c constraints/desktop.txt`, `.venv/bin/python -m py_compile build_desktop.py desktop_doctor.py`, `.venv/bin/python -m unittest tests.test_web_assets tests.test_desktop_doctor tests.test_desktop_share` (59 OK) y `git diff --check` OK. Impacto: builds desktop/CI quedan más reproducibles y el próximo upgrade de PyInstaller/pywebview/Qt queda aislado como tarea explícita, no mezclada con features; Windows/macOS siguen requiriendo smoke nativo cuando haya host.
- 2026-05-05: Quick win P2 desktop/readiness fallback web dirigido cerrado. `steam_tools_desktop.py` agrega `STEAM_TOOLS_FORCE_WEB_FALLBACK=1` y `--force-web-fallback` para validar el modo navegador sin romper ni desinstalar pywebview/WebView2/Qt; el launcher espera/reusa el server local, abre el navegador con `?desktop_fallback=1&reason=forced-web-fallback`, imprime siempre `URL fallback: ...` para evidencia determinística y deja vivo el server en modo fallback. `web/steam_deals/app.js` agrega hint visible para `forced-web-fallback`, y los runbooks Linux/Windows/macOS documentan el flujo forzado y la mitigación manual. Evidencia: `.venv/bin/python -m py_compile steam_tools_desktop.py`, `.venv/bin/python -m unittest tests.test_desktop_share tests.test_web_assets tests.test_config_security tests.test_host_loopback_steam_deals tests.test_host_loopback_payday2 tests.test_shared_web_infra tests.test_generated_files_serving` (141 OK), `git diff --check` OK, y smoke source/headless con `BROWSER=/bin/false .venv/bin/python steam_tools_desktop.py --force-web-fallback` confirmó `Fallback web forzado...` y `URL fallback: http://127.0.0.1:8080?desktop_fallback=1&reason=forced-web-fallback`; el entorno headless imprimió warnings Qt/Wayland no bloqueantes al intentar abrir navegador. Impacto: fallback web queda como modo degradado soportado y testeable sin rebuild ni runs largos; falta solo validación visual en host gráfico para cierre cross-platform manual.
- 2026-05-04: Quick win P0 seguridad Host loopback/DNS rebinding cerrado. `shared_web_infra` agrega validación stdlib de `Host` (`is_valid_loopback_host`, `is_valid_loopback_host_header`) y payload 403 seguro (`forbidden_host`) que no refleja host crudo ni detalles internos. Steam Deals Web y PAYDAY 2 Web ejecutan la guardia al inicio de `do_GET`/`do_POST`, antes de servir UI/API/files/assets o ejecutar side effects, aceptando solo `localhost`, `127.0.0.1` y `[::1]` con el puerto activo; se rechazan Host faltante, malformado, externo, LAN/private, `0.0.0.0` o con puerto incorrecto. Evidencia: `python3 -m py_compile shared_web_infra.py steam_deals_web.py payday2_web.py steam_tools_desktop.py app/steam_deals_config.py payday2_dlc_tracker.py`, `python3 -m unittest tests.test_shared_web_infra tests.test_host_loopback_steam_deals tests.test_host_loopback_payday2 tests.test_config_security tests.test_generated_files_serving tests.test_web_assets tests.test_desktop_share tests.test_generator_logic.ConfigTests` (145 OK) y `git diff --check` OK. Impacto: se completa la capa complementaria a anti-CSRF para que el server local no atienda requests con Host no loopback; el frente P0 seguridad/local server queda cerrado salvo regresiones futuras.
- 2026-05-04: Quick win **Copiar log** universal en desktop cerrado. Se descartó depender de `wl-copy`/`xclip`/`xsel` porque no son universales; `steam_tools_desktop.py` ahora expone `DesktopClipboardApi` por `js_api` de pywebview y copia con `QApplication.clipboard().setText()`, la ventana nativa carga la UI con `desktop_native=1`, y `web/steam_deals/app.js` usa primero `window.pywebview.api.copy_text_to_clipboard(text)`; en navegador normal cae a `navigator.clipboard.writeText(text)` y, si falla, muestra `Usa Descargar log (.txt)`. Se eliminó el endpoint `/api/log/copy` y el flujo de logs ya no usa `document.execCommand` ni `window.prompt`. Evidencia: `.venv/bin/python -m py_compile steam_tools_desktop.py steam_deals_web.py`, `.venv/bin/python -m unittest tests.test_web_assets tests.test_generated_files_serving tests.test_config_security tests.test_desktop_share tests.test_shared_web_infra tests.test_desktop_doctor tests.test_runtime_paths` (145 OK), `git diff --check` OK, `QT_QPA_PLATFORM=offscreen` smoke del helper Qt con readback OK, rebuild `.venv/bin/python build_desktop.py --skip-install` OK con warnings no fatales conocidos (`libtiff.so.5`, PyInstaller/root), y smoke del binario rebuilt `--internal-web` confirmó assets con `desktop_native`, `copy_text_to_clipboard`, fallback `navigator.clipboard.writeText(text)`, ausencia de `/api/log/copy` en JS y `POST /api/log/copy` = 404. Impacto: **Copiar log** vuelve a copiar realmente en desktop sin depender de paquetes del sistema; falta solo retest visual/manual de la ventana nativa para confirmar el clipboard real en la sesión gráfica del usuario.
- 2026-05-04: Quick win P0 seguridad anti-CSRF local cerrado. Se agregó `LOCAL_SESSION_TOKEN` por proceso y helpers compartidos en `shared_web_infra` para comparar `X-Steam-Tools-Local-Token` con `hmac.compare_digest`, validar `Origin`/`Referer` contra loopback (`localhost`, `127.0.0.1`, `[::1]`) y puerto activo, tolerar ausencia de Origin/Referer con token válido para pywebview/desktop, y responder 403 seguro. Steam Deals Web protege POST mutables (`run`, `run-pd2`, `preflight`, Doctor/Autofix, cache clear, stop, open output folder, config, watchlist, log export) y PAYDAY 2 Web protege `toggle`, `toggle-bundle`, `refresh` y config; ambos inyectan el token solo en la UI raíz servida localmente y sus JS usan `localMutableFetch` para mutaciones. Evidencia: `python3 -m py_compile shared_web_infra.py steam_deals_web.py payday2_web.py steam_tools_desktop.py app/steam_deals_config.py payday2_dlc_tracker.py`, `python3 -m unittest tests.test_shared_web_infra tests.test_config_security tests.test_generated_files_serving tests.test_web_assets tests.test_desktop_share tests.test_generator_logic.ConfigTests` (125 OK), `git diff --check` OK. `CodeReviewer` abortó repetidamente, así que se hizo revisión manual por partes (helpers, Steam Deals, PAYDAY 2) sin blockers. Impacto: POST mutables del server local ya no son invocables por páginas externas sin el token local; Host/loopback contra DNS rebinding queda como hardening separado.
- 2026-05-04: Quick win de output default persistente en binario frozen cerrado. Se detectó durante validación nativa que un run sin carpeta de salida explícita podía lanzar el generator con `--output /tmp/_MEI.../output`, dejando reportes en el directorio temporal de PyInstaller. Cambio: `app/steam_deals_paths.py` agrega `STEAM_DEALS_OUTPUT_DIR`, `resolve_user_output_dir()` y `resolve_reports_output_dir()`; `build_persistent_runtime_env()` inyecta output persistente en frozen si no hay override; `steam_deals_web.resolve_output_dir()` usa ese helper solo cuando el campo output está vacío, preservando source mode (`output/`) y salidas explícitas; `app/steam_deals_config.py` usa el mismo default persistente para ejecución frozen directa sin `--output`. Evidencia: `.venv/bin/python -m py_compile app/steam_deals_paths.py app/steam_deals_config.py steam_tools_desktop.py steam_deals_web.py`, `.venv/bin/python -m unittest tests.test_runtime_paths tests.test_generated_files_serving tests.test_config_security tests.test_desktop_share tests.test_desktop_doctor` (92 OK), `git diff --check` OK, rebuild `build_desktop.py --skip-install` OK con warnings no fatales conocidos (`libtiff.so.5`, PyInstaller/root), y smoke del binario rebuilt con `env -u STEAM_DEALS_CACHE_DIR -u STEAM_DEALS_LOG_DIR -u STEAM_DEALS_OUTPUT_DIR HOME=.tmp/.../frozen-output-home dist/SteamToolsDesktop --internal-web --no-open --port 8100`: `/api/config` 200, Doctor frozen OK, preflight OK y run pequeño `joseluis12351` exit 0 generó `.md/.html/.json/.csv/share.html` en `.tmp/.../frozen-output-home/SteamTools/output`, fuera de `_MEI`; `pgrep` no dejó procesos colgados. Impacto: cache, logs y ahora outputs default del binario frozen quedan persistentes sin requerir env vars manuales; queda solo retest visual/manual de ventana nativa y **Copiar log** si se quiere cerrar UX Linux final.
- 2026-05-04: Cierre final del quick win P0 seguridad de saneo de errores públicos. Tras el review se corrigieron los últimos riesgos de filtración en respuestas success (`/api/config`, abrir carpeta, export log), líneas SSE/progreso de Steam Deals y PAYDAY 2, secretos guardados/env como `extra_values` y `done.files`. Evidencia: `python3 -m py_compile shared_web_infra.py steam_deals_web.py payday2_web.py steam_tools_desktop.py app/steam_deals_config.py payday2_dlc_tracker.py`, `python3 -m unittest tests.test_shared_web_infra tests.test_generated_files_serving tests.test_config_security tests.test_desktop_share tests.test_generator_logic.ConfigTests tests.test_web_assets` (101 OK), `git diff --check` OK y CodeReviewer final sin blockers. Impacto: el slice queda seguro para marcar completo; anti-CSRF y Host/loopback siguen como hardenings separados.
- 2026-05-04: Fix intermedio de crash Linux/pywebview al usar **Copiar log** después de un run desktop. Diagnóstico: el primer fix seguía permitiendo clipboard en logs cortos y el usuario confirmó que **Copiar log** aún crasheaba; como mitigación temporal, `copyExecutionLog()` pasó a guardar el log vía `/api/log/export` sin `copyTextWithFallback`, `navigator.clipboard`, `execCommand` ni `window.prompt` dentro del flujo de logs. Evidencia de esa mitigación: `.venv/bin/python -m unittest tests.test_web_assets tests.test_generated_files_serving tests.test_desktop_share tests.test_desktop_doctor tests.test_runtime_paths` (95 OK), `git diff --check` OK, rebuild `build_desktop.py --skip-install` OK y smoke del binario rebuilt confirmó export seguro con token local anti-CSRF. Impacto: evitó el crash, pero el usuario rechazó correctamente que **Copiar log** dejara de copiar; quedó supersedido el mismo día por el puente nativo pywebview/PyQt documentado arriba.
- 2026-05-04: Rebuild/smoke del binario frozen sin env vars manuales completado. Se reconstruyó `dist/SteamToolsDesktop` con `.venv/bin/python build_desktop.py --skip-install` (warning no fatal conocido: `libtiff.so.5` en plugin Qt TIFF; PyInstaller también advierte por ejecución como root). El binario ejecutado con `env -u STEAM_DEALS_CACHE_DIR -u STEAM_DEALS_LOG_DIR dist/SteamToolsDesktop --doctor` confirmó `Runtime frozen` OK, `Cache/logs persistentes` OK, `Entorno Python`/`pywebview`/`PyInstaller` como no aplicables/OK y solo warning por validación root/display. El primer smoke funcional pequeño vía `--internal-web` falló con `KeyError: 'name'` en `app/steam_deals_prices._build_deals` cuando una entrada de fallo de caché pasaba el filtro `min_discount=0`; se corrigió `_build_deals()` para ignorar entradas de fallo/malformadas antes de crear deals y se agregó test de regresión. Evidencia del fix: `.venv/bin/python -m py_compile app/steam_deals_prices.py`, `.venv/bin/python -m unittest tests.test_generator_logic.WarmCacheTests tests.test_runtime_paths tests.test_desktop_share tests.test_desktop_doctor` (54 OK), regresión relevante `tests.test_config_security tests.test_generated_files_serving tests.test_shared_web_infra tests.test_web_assets tests.test_runtime_paths tests.test_desktop_share tests.test_desktop_doctor tests.test_generator_logic.WarmCacheTests` (123 OK) y `git diff --check` OK. Tras rebuild, el smoke headless/internal-web del binario sin env vars manuales respondió `/api/config` 200, `POST /api/desktop-doctor` 200 (`Runtime frozen`/`Cache/logs persistentes` OK), preflight OK y run pequeño `https://steamcommunity.com/id/joseluis12351` terminó con exit 0 generando `Steam Deals Now Available - Far Far West 2026-05-04.md/.html/.json/.csv` y `Steam Deals Share 2026-05-04.html` en `.tmp/sessions/.../smoke-output-after-fix`; `pgrep` no mostró procesos colgados salvo el propio comando de inspección. Impacto: queda validado el runtime frozen rebuilt sin depender de `STEAM_DEALS_CACHE_DIR`/`STEAM_DEALS_LOG_DIR`; para cierre UX nativo final todavía conviene abrir ventana normal desde usuario gráfico no-root si se decide exigir esa evidencia.
- 2026-04-30: Quick win desktop frozen runtime cerrado: `app/steam_deals_paths.py` agrega defaults persistentes por plataforma para frozen (`${XDG_CACHE_HOME:-~/.cache}/steam_deals` en Linux, `%LOCALAPPDATA%/SteamTools/cache` en Windows y `~/Library/Caches/SteamTools` en macOS) y `build_persistent_runtime_env()` solo inyecta `STEAM_DEALS_CACHE_DIR`/`STEAM_DEALS_LOG_DIR` cuando no hay overrides. `steam_tools_desktop.py` pasa esos env defaults al server hijo frozen junto con `PYINSTALLER_RESET_ENVIRONMENT=1`, y `steam_deals_web.py` los mantiene para subprocesses generator/PAYDAY 2 en modo frozen. `desktop_doctor.py` ahora reporta `Runtime frozen` y `Cache/logs persistentes`, trata `_MEIPASS` como raíz temporal de recursos, falla si cache/logs apuntan allí, y evita falsos FAIL source-only de `Entorno Python`, `pywebview`, `PyInstaller`, build config/artifact dentro del binario. Evidencia: `python3 -m py_compile app/steam_deals_paths.py steam_tools_desktop.py steam_deals_web.py desktop_doctor.py tests/test_runtime_paths.py tests/test_desktop_share.py tests/test_desktop_doctor.py`, `python3 -m unittest tests.test_runtime_paths tests.test_desktop_share tests.test_desktop_doctor` (42 OK), `python3 -m unittest tests.test_config_security tests.test_generated_files_serving tests.test_shared_web_infra tests.test_web_assets tests.test_runtime_paths tests.test_desktop_share tests.test_desktop_doctor` (111 OK) y `git diff --check` OK. Impacto: se cierra el caveat P2 del binario que dependía de env vars manuales para cache/logs y del Doctor que diagnosticaba el Python/pywebview/PyInstaller del sistema como bloqueo aunque el runtime frozen ya estuviera operativo; queda validar un binario rebuilt sin env vars cuando toque smoke desktop/manual.
- 2026-04-30: Quick win P0 seguridad saneo de errores públicos cerrado: `shared_web_infra` suma `redact_sensitive_text()` y `safe_public_error_payload()` para remover keys/tokens/webhooks, Discord webhooks, tokens Telegram, rutas absolutas POSIX/Windows, `_MEIPASS` y marcadores `Traceback`. Se aplicó en errores públicos de Steam Deals Web para abrir carpeta, Desktop Doctor/Autofix, exportar logs, preflight, arranque de subprocess, líneas SSE runtime, `done.files` y páginas `/files`, y en PAYDAY 2 Web para errores de arranque del refresh y líneas SSE runtime. Los mensajes públicos conservan status y copy accionable, moviendo detalle saneado a `detail` cuando aplica. Tras review, se amplió redacción para rutas como `/usr/bin/python3`, `/etc/hosts`, `/srv/app/...` y `/private/tmp/...`, y se cubrieron preflight/SSE. Evidencia: `python3 -m py_compile shared_web_infra.py steam_deals_web.py payday2_web.py steam_tools_desktop.py app/steam_deals_config.py payday2_dlc_tracker.py`, `python3 -m unittest tests.test_shared_web_infra tests.test_generated_files_serving tests.test_config_security tests.test_desktop_share tests.test_generator_logic.ConfigTests tests.test_web_assets` (100 OK) y `git diff --check` OK. Impacto: se cierra el Track P0 seguridad/local server original; quedan como hardenings separados anti-CSRF y validación Host/loopback.
- 2026-04-30: Quick win P0 seguridad `--run-script` allowlist cerrado: `steam_tools_desktop.py` define `ALLOWED_EMBEDDED_SCRIPTS = {"steam_deals_generator.py", "payday2_dlc_tracker.py"}` y agrega `validate_embedded_script_name()`/`resolve_allowed_embedded_script()` antes de ejecutar `runpy.run_path`. Se rechazan vacío, rutas absolutas, `..`, separadores `/`/`\\`, `:`, drive Windows y nombres no allowlisted antes de tocar `runpy`; los scripts permitidos conservan passthrough args. Tras review, `main()` también captura `ValueError`/`RuntimeError`, imprime solo el mensaje seguro y sale con código `2`, evitando tracebacks/rutas absolutas para usuarios. Evidencia: `python3 -m py_compile steam_tools_desktop.py steam_deals_web.py shared_web_infra.py payday2_web.py app/steam_deals_config.py payday2_dlc_tracker.py`, `python3 -m unittest tests.test_desktop_share tests.test_generated_files_serving tests.test_config_security tests.test_generator_logic.ConfigTests tests.test_shared_web_infra tests.test_web_assets` (96 OK) y `git diff --check` OK. Impacto: se cierra otro slice del Track P0 seguridad/local server; tras el saneo de errores públicos, quedan como hardenings separados anti-CSRF y validación Host/loopback.
- 2026-04-30: Quick win P0 seguridad CSP sandbox para `/files/*.html` cerrado: se agrega `GENERATED_HTML_CSP` y `generated_html_security_headers()` en `steam_deals_web.py`. Los HTML generados servidos desde `/files/*.html` mantienen `Content-Disposition: inline`, pero reciben `Content-Security-Policy` con `sandbox allow-scripts allow-popups allow-downloads` sin `allow-same-origin`, `default-src 'none'`, `script-src 'unsafe-inline'`, `style-src 'unsafe-inline'`, `img-src https: data:`, `connect-src 'none'`, `form-action 'none'`, `base-uri 'none'`, `object-src 'none'`, `frame-ancestors 'none'` y `Referrer-Policy: no-referrer`; MD/JSON/CSV no reciben CSP sandbox y conservan `attachment`. Evidencia: `python3 -m py_compile steam_deals_web.py shared_web_infra.py payday2_web.py app/steam_deals_config.py payday2_dlc_tracker.py`, `python3 -m unittest tests.test_generated_files_serving tests.test_config_security tests.test_generator_logic.ConfigTests tests.test_shared_web_infra tests.test_desktop_share tests.test_web_assets` (83 OK) y `git diff --check` OK. Nota: se intentó CodeReviewer, pero la herramienta abortó; no se relanzó para evitar espera larga. Impacto: se cierra otro slice del Track P0 seguridad/local server; quedan saneo de errores públicos, allowlist de `--run-script`, anti-CSRF/host.
- 2026-04-30: Benchmark BG00G con fallback adaptativo `STEAM_DEALS_INDIVIDUAL_FALLBACK_WORKERS=2` completado. Comando ejecutado con cache aislado `STEAM_DEALS_CACHE_DIR="$HOME/.cache/steam_deals-adaptive-workers2-$(date +%Y%m%d-%H%M%S)"`, pero en la shell del benchmark `$HOME` resolvió a `/root`, dejando evidencia en `/root/.cache/steam_deals-adaptive-workers2-20260430-120912/logs/warm-cache-2026-04-30_12-09-12.log`. Resultado: `4678.9s`, wishlist `2,934`, deals `401`, refresh candidates `2,934`, `21` batches HTTP 400, fallback individual `2,934` juegos en `168` tandas (`2,419` resueltos, `515` sin oferta/datos), fallback directo HTTP 400 `2,874` juegos en `144` tandas, `1` bajada adaptativa de workers y razones `http_429=111`, `no_price_data=404`. Comparación: contra serial baseline (`4844.7s`, `456` deals, `2,499` resueltos, `434` sin datos) solo ahorra ~`166s` y baja calidad; contra workers puros (`4`: `1228.2s`/`114` deals, `2`: `2423.7s`/`229` deals) conserva mucha más calidad. Impacto: confirma que el adaptativo es un guardrail útil para experimentos, pero `STEAM_DEALS_INDIVIDUAL_FALLBACK_WORKERS` debe seguir en default `1`; el siguiente trabajo de performance debe ir por presupuesto adaptativo/stale-while-revalidate/presupuesto de tiempo antes de promo-cache.
- 2026-04-30: Quick win P0 seguridad `/files/...` cerrado: se agrega allowlist estricta en `steam_deals_web.py` para listar/servir solo artefactos generados esperados. Los reportes Steam Deals primarios deben tener fecha y grupo completo `.md/.html/.json`, el Share HTML se permite solo como `Steam Deals Share YYYY-MM-DD.html` y PAYDAY 2 solo con nombres exactos `PAYDAY2_Plan_de_Compra.html/.md/.csv`. Se bloquean traversal, rutas anidadas, extensiones desconocidas, symlinks, directorios, basenames no generados y nombres generados-falsos como `Steam Deals secrets.json`, `Steam Deals secrets YYYY-MM-DD.json`, `Steam Deals Share YYYY-MM-DD.json` y backups con doble extensión. HTML conserva `inline` y MD/JSON/CSV conservan `attachment`. Evidencia: `python3 -m py_compile steam_deals_web.py shared_web_infra.py payday2_web.py app/steam_deals_config.py payday2_dlc_tracker.py`, `python3 -m unittest tests.test_generated_files_serving tests.test_config_security tests.test_generator_logic.ConfigTests tests.test_shared_web_infra tests.test_desktop_share tests.test_web_assets` (81 OK), `git diff --check` OK y CodeReviewer sin findings. Impacto: se cierra el tercer slice del Track P0 seguridad/local server; quedan CSP sandbox para HTML generado, saneo de errores públicos, allowlist de `--run-script`, anti-CSRF/host.
- 2026-04-30: Track Performance / fallback adaptativo: se agrega diagnóstico y guardrail para `STEAM_DEALS_INDIVIDUAL_FALLBACK_WORKERS>1`. `fetch_single` ya marca errores como `http_<code>`/`error_<tipo>` en vez de ocultarlos como `None`; el fallback individual acumula razones de fallo, guarda `_failure_reason`, y si una tanda paralela supera el umbral de fallos baja automáticamente a `1` worker para preservar calidad. `steam_deals_generator.py` emite `Fallback individual adaptativo` y `Fallback individual fallos por razón`; `steam_deals_warm_cache_summary.py` parsea/formatea esos datos, y el runbook warm-cache agrega checklist/plantilla para compararlos. Evidencia: `.venv/bin/python -m pytest tests/test_generator_logic.py -k "price_cache or fallback or cooldown or http_400" tests/test_warm_cache_summary.py` (30 selected OK, 156 deselected). Impacto: workers paralelos quedan como opt-in defensivo/diagnóstico mientras se compara calidad; siguiente seguimiento recomendado es correr BG00G cold-cache aislado con `STEAM_DEALS_INDIVIDUAL_FALLBACK_WORKERS=2` adaptativo y comparar contra serial `4844.7s`/`456` deals, workers=2 puro `2423.7s`/`229` deals y workers=4 puro `1228.2s`/`114` deals.
- 2026-04-30: Quick win P0 seguridad secretos fuera de `argv` cerrado: se agrega `CONFIG_SECRET_ENV_VARS`, `extract_secret_env`, `config_without_secrets`, `build_secret_subprocess_env` y `resolve_config_secret` en `shared_web_infra`. Steam Deals Web y PAYDAY 2 Web ya construyen comandos de subprocess sin valores ni flags sensibles (`--key`, `--itad-key`, `--telegram-token`, `--discord-webhook`) y pasan los secretos por env (`STEAM_TOOLS_STEAM_API_KEY`, `STEAM_TOOLS_ITAD_API_KEY`, `STEAM_TOOLS_TELEGRAM_TOKEN`, `STEAM_TOOLS_DISCORD_WEBHOOK`). `app/steam_deals_config.py` y `payday2_dlc_tracker.py` resuelven secretos por orden `flag CLI > env > config guardada`, preservando compatibilidad manual. Evidencia: `python3 -m py_compile shared_web_infra.py steam_deals_web.py app/steam_deals_config.py steam_deals_generator.py payday2_web.py payday2_dlc_tracker.py`, `python3 -m unittest tests.test_config_security tests.test_generator_logic.ConfigTests tests.test_shared_web_infra tests.test_desktop_share tests.test_generated_files_serving tests.test_web_assets` (74 OK), `git diff --check` OK y CodeReviewer sin findings. Impacto: se cierra el segundo slice del Track P0 seguridad/local server; quedan hardenings de `/files`, CSP sandbox, errores públicos, `--run-script`, anti-CSRF/host.
- 2026-04-30: Quick win P0 seguridad `/api/config` cerrado: se agrega redacción compartida en `shared_web_infra` para `key`, `itad_key`, `telegram_token` y `discord_webhook`; Steam Deals y PAYDAY 2 devuelven config pública con flags `has_key`/`has_itad_key`/`has_telegram_token`/`has_discord_webhook` sin valores crudos. `POST /api/config` preserva secretos guardados cuando el cliente manda valores vacíos, `null` o marcadores redactados, pero permite reemplazo cuando llega un secreto nuevo explícito. Steam Deals además hidrata secretos guardados para preflight/run después de recibir config pública, evitando que la redacción rompa ejecuciones existentes. Evidencia: `python3 -m py_compile shared_web_infra.py steam_deals_web.py payday2_web.py` OK y `python3 -m unittest tests.test_config_security tests.test_shared_web_infra tests.test_desktop_share tests.test_generated_files_serving tests.test_web_assets` (60 OK). Impacto: se cierra el primer slice del Track P0 seguridad/local server.
- 2026-04-29: Quick win `Detener` desktop cerrado: el usuario validó desde el binario desktop que un run inició, el botón `Detener` funcionó correctamente, no duplicó el mensaje `Solicitando detener ejecucion...`, no dejó la app congelada y el cierre fue correcto. Se revisó el log descargado `/home/adolfo/Documents/Deals/logs/steam-deals-log-2026-04-28T21-31-21.txt`; corresponde a `Desktop Doctor ejecutado`, no al stop, y muestra el caveat conocido del Doctor dentro/fuera del runtime (`/usr/bin/python3`, `pywebview` faltante, `PyInstaller` faltante, validación/root) aunque el artefacto `dist/SteamToolsDesktop` existe. Impacto: se cierra la evidencia pendiente de `Detener` en desktop; los falsos bloqueos del Doctor quedan como seguimiento separado del track desktop/readiness.
- 2026-04-29: Track Performance agrega fallback individual concurrente opt-in para cold-cache grande: `STEAM_DEALS_INDIVIDUAL_FALLBACK_WORKERS` permite usar hasta `4` workers solo en fallback individual, manteniendo default serial `1`, cache/cooldown y stats existentes; `resolve_price_fetch_tuning` lo reporta en `Tuning precios activo` y el resumen offline parsea `fallback_workers`. Evidencia: `.venv/bin/python -m pytest tests/test_generator_logic.py -k "price_cache or fallback or cooldown or http_400" tests/test_warm_cache_summary.py` (28 selected OK, 153 deselected). Impacto: siguiente experimento puede comparar el baseline BG00G cold `4844.7s` contra una corrida aislada con `STEAM_DEALS_INDIVIDUAL_FALLBACK_WORKERS=4` sin cambiar el comportamiento por defecto.
- 2026-04-29: Quick win benchmark BG00G cold-vs-hot cache cerrado con cache aislado `BASELINE_CACHE=/home/adolfo/.cache/steam_deals-user-cold-baseline-20260429-111134`. Primera corrida warm-cache cold (`warm-cache-2026-04-29_11-11-34.log`): `4844.7s`, wishlist `2,933`, deals `456`, cache `empty`, refresh candidates `2,933` (`2,933` nuevos), `21` batches HTTP 400, fallback individual `2,933` juegos en `168` tandas (`2,499` resueltos, `434` sin oferta/datos), fallback directo HTTP 400 `2,873` juegos en `144` tandas. Segunda corrida inmediata hot (`warm-cache-2026-04-29_13-05-55.log`): `1.3s`, cache `valid (0.6h)`, refresh candidates `0`, cooldown `434`, HTTP 400 `0`, fallback `0`. Comparación: `-4843.4s`, `-2,933` refresh candidates, `-21` HTTP 400 y `-2,933` fallback total; `Warm-cache next actions`: `Cache efectivo: los refresh candidates bajaron fuerte; conserva cache caliente antes de tocar promos.` Impacto: confirma con perfil grande real que el cache caliente funciona; el problema pendiente queda acotado a costo de cold/stale refresh y fallback directo, no a hot-cache. Siguiente seguimiento: usar este cache para outputs grandes/Shuffle si se necesita evidencia UX, o retomar performance solo con objetivo explícito.
- 2026-04-29: Criterio operativo de benchmarks definido: `https://steamcommunity.com/id/joseluis12351` queda como wishlist pequeña para smokes rápidos, Share/Compartir E2E y outputs básicos; `BG00G` queda como wishlist grande para performance cold-vs-hot, stress de outputs, Shuffle con más candidatos y desktop/binario largo. Regla: no usar `BG00G` para quick wins si implica cold-cache largo salvo que el objetivo explícito sea medir performance con cache controlado.
- 2026-04-29: Track Share/Compartir E2E cerrado con reporte pequeño: se generó un run con `STEAM_DEALS_CACHE_DIR="/home/adolfo/.cache/steam_deals-small" python3 steam_deals_generator.py --vanity "https://steamcommunity.com/id/joseluis12351" --csv`, wishlist de `9` juegos, `1` deal (`DREDGE`) y duración `20.8s`. Artefactos: `Steam Deals Now Available - Far Far West 2026-04-29.md`, `.html`, `.json`, `.csv` y `Steam Deals Share 2026-04-29.html`. Validación estática confirmó payload share con `appid`, nombre, precio, precio original, descuento y URL; el usuario validó manualmente que HTML interactivo y Share HTML abren, botón compartir abre modal, `Copiar link steamtools://`, `Copiar link de Steam`, `Abrir en Steam` y `Cerrar` funcionan sin errores visibles. Impacto: se cierra la validación manual final del track Share/Compartir. Siguiente seguimiento: ninguno salvo regresiones futuras.
- 2026-04-28: Track Linux desktop binario avanza con evidencia E2E real: tras reinstalar deps desktop en `.venv`, `steam_tools_desktop.py --doctor` quedó sin FAIL reales salvo warning por validación/root y el build `build_desktop.py` generó `/home/adolfo/Documents/Deals/dist/SteamToolsDesktop`. El usuario abrió el binario en Linux con ventana nativa y lo relanzó con `STEAM_DEALS_CACHE_DIR="$HOME/.cache/steam_deals" STEAM_DEALS_LOG_DIR="$HOME/.cache/steam_deals/logs" /home/adolfo/Documents/Deals/dist/SteamToolsDesktop`; la corrida usó cache caliente (`warm-cache-2026-04-28_13-58-22.log`: `Caché válida (0.7h) — sin nuevos, skip fetch (411 fallos recientes en cooldown)`, `Warm cache listo en 1.3s`) y generó `Steam Deals Now Available - Far Far West 2026-04-29.md/.html/.csv/.json` + `Steam Deals Share 2026-04-29.html`. Cierre manual OK y `ps aux | grep -E "SteamToolsDesktop|steam_deals|steam_tools" | grep -v grep` no imprimió procesos colgados. Incidencias/follow-ups: al abrir sin env vars el desktop intentaba un refresh largo (~2,870 candidatos); el Doctor dentro del binario reportó `/usr/bin/python3` y `pywebview` faltante, probablemente falso diagnóstico del runtime congelado; revisar default de cache/log dir del desktop o documentar launcher con env vars.
- 2026-04-28: Quick win auth Steam CLI cerrado: el smoke post-warm detectó primero `HTTP 403` en `ResolveVanityURL`; se agregó fallback público al resolver Steam ID cuando la API key es rechazada, con error accionable si el perfil público también falla. El retry avanzó hasta biblioteca propia y detectó `HTTP 401`; `get_owned_games` ahora convierte `401/403` en `ValueError` accionable y la CLI continúa con biblioteca vacía en vez de traceback crudo. Evidencia: `python3 -m py_compile app/steam_deals_steam_api.py steam_deals_generator.py` OK y `python3 -m unittest tests.test_generator_logic.SteamAdapterTests` (16 OK). Resultado operativo: los errores auth ya no bloquean antes de performance; el intento posterior BG00G warm-cache siguió largo por `2,933` refresh candidates, HTTP 400 repetido/fallback individual y ETA ~80m, por lo que se abortó manualmente. Siguiente seguimiento: no insistir con BG00G cold/stale sin ventana larga o nuevo ajuste de performance.
- 2026-04-28: Track Performance / cold-cache throughput: se agrega un circuit breaker conservador al fetching de precios para que, tras HTTP 400 repetido en batches, deje de gastar splits `20→10→5` y use fallback individual directo para las tandas restantes; también se evita sleep extra antes de fallback forzado por HTTP 400, se usa timestamp por batch/fallo y el resumen offline parsea `Fallback individual directo por HTTP 400 repetido`. Evidencia: usuario recreó `.venv`, instaló `pytest` local y validó con `.venv/bin/python -m pytest tests/test_generator_logic.py -k "price_cache or fallback or cooldown or http_400" tests/test_warm_cache_summary.py` (23 selected OK, 149 deselected, warning no bloqueante de permisos en `.pytest_cache`). Impacto: apunta al costo real de cache frío/expirado sin tocar hot-cache ni promo-cache. Siguiente seguimiento: próxima corrida cold/stale grande para comparar duración, HTTP 400 y fallback directo vs los logs base de BG00G.
- 2026-04-28: Quick win evidencia warm-cache real cerrada: se resumieron los logs `/home/adolfo/.cache/steam_deals/logs/warm-cache-2026-04-28_13-00-49.log`, `warm-cache-2026-04-28_13-39-05.log` y `warm-cache-2026-04-28_13-39-56.log` con `python3 steam_deals_warm_cache_summary.py ...`. Resultado: wishlist `2,933`, deals `478`; corrida stale/cold parcial `436` refresh candidates (`0` nuevos, `436` stale), `154` batches degradados HTTP 400, fallback individual `436` juegos en `176` tandas (`25` resueltos, `411` sin oferta/datos), duración `1107.3s`; corridas posteriores con cache caliente/cooldown quedaron en `0` refresh candidates, `411` cooldown, `0` HTTP 400, `0` fallback y `1.2s`/`1.4s` (`batch_size=20` y luego `batch_size=10`, `halving_limit=3`). El bloque `Warm-cache next actions` quedó en `Sin acción automática` porque las métricas calientes están estables. Evidencia adicional: `python3 -m unittest tests.test_warm_cache_summary` (17 OK). Impacto: confirma que cache/cooldown evita reintentos inmediatos; el costo pendiente está en refresh stale/cold y fallback individual cuando Steam degrada batches. Siguiente seguimiento: seguir con quick wins o smoke desktop con cache caliente; no cambiar política promo-cache todavía.
- 2026-04-28: Quick win icono desktop/Windows avanza sin interferir con warm-cache: se agrega `assets/steam_tools_icon.svg`, `build_desktop.py` genera un `.ico` determinístico con stdlib para builds Windows y pasa `--icon` a PyInstaller, `SteamToolsDesktop.spec` queda alineada con `icon=...` en Windows y `desktop_doctor.py`/tests cubren el asset. Evidencia: `python3 -m py_compile build_desktop.py desktop_doctor.py` OK y `python3 -m unittest tests.test_web_assets` (18 OK). Impacto: el build Windows ya no queda sin wiring de icono propio. Siguiente seguimiento: validar en host Windows que `dist\\SteamToolsDesktop.exe` y la ventana muestran el icono de Steam Tools y no el genérico de Python.
- 2026-04-28: Validación manual post-implementación del budget PAYDAY 2: se reinició `payday2_web.py --no-open`, `/` y `/api/data` respondieron 200 en `http://127.0.0.1:8081`, y el usuario confirmó en browser que `Tu Presupuesto Ideal` muestra Tier, funciona perfecto y el ranking por importancia/valor no se siente exagerado ni fuera de lugar. Impacto: refuerza el cierre del quick win con evidencia visual real. Siguiente seguimiento: ninguno, salvo calibración futura si aparecen casos raros con otros datos.
- 2026-04-28: Quick win PAYDAY 2 `Tu Presupuesto Ideal` por importancia/valor cerrado: se agrega scoring determinístico y explicable para DLCs (`S` heists/contenido jugable, `A` armas/mods, `B` personaje/general, `C` cosmético/audio/suscripción), el presupuesto usa `valueScore`/`importanceScore` antes que descuento/precio, Markdown y dashboard muestran prioridad/razones, y `/api/data` expone metadata de valor para la UI. Evidencia: `python3 -m py_compile payday2_dlc_tracker.py payday2_web.py`, `python3 -m unittest tests.test_web_assets` (17 OK) y `python3 -m unittest tests.test_shared_web_infra tests.test_shared_cache_utils tests.test_runtime_paths` (19 OK). Nota operativa: `pytest` no estaba disponible en system Python y no existía `.venv`, por eso se validó con `unittest`. Siguiente seguimiento: calibrar recomendaciones PAYDAY 2 más finas con uso real si aparecen casos raros de ranking.
- 2026-04-28: Quick win PAYDAY 2 visual real-data cerrado: se levantó/verificó `payday2_web.py --no-open` en `http://127.0.0.1:8081` con `/` y `/api/data` respondiendo OK; el usuario validó en browser que el dashboard responde con datos reales, las acciones probadas salieron bien y no hubo errores visibles. Impacto: se cierra la validación visual/manual pendiente antes de expandir alcance. Siguiente seguimiento: nuevo quick win para que `Tu Presupuesto Ideal` de PAYDAY 2 priorice importancia/valor del DLC en vez de solo ordenar por precio/descuento.
- 2026-04-28: Track Performance / warm-cache recommendations: se afina el bloque `Warm-cache next actions` para que HTTP 400 repetido en `batch_size=1` no sugiera un valor imposible y para que el warning genérico `fallback-still-high` no duplique una acción más específica de cooldown por fallback sin datos. Evidencia: `.venv/bin/python -m pytest tests/test_warm_cache_summary.py` (16 tests OK) y `.venv/bin/python -m pytest tests/test_generator_logic.py -k "warm_cache or price_cache or fallback or cooldown" tests/test_runtime_paths.py tests/test_shared_cache_utils.py` (17 selected OK). Impacto: el Track Performance queda con recomendaciones offline más claras antes de medir corrida larga real. Siguiente seguimiento: corrida larga warm-cache/binario para validar reducción real de fallback y duración.
- 2026-04-27: Fix PAYDAY 2 ownership ante Steam HTTP 401/403: `get_owned_games` ahora convierte rechazo de API key en error accionable y `resolve_owned_dlc_appids` conserva el ownership manual/cache para que el refresh continúe sin traceback crudo. Validado con `tests/test_web_assets.py -k "payday2 or Payday2SteamResolution"` (7 tests OK) y regresión relacionada `tests/test_web_assets.py tests/test_shared_web_infra.py tests/test_shared_cache_utils.py tests/test_runtime_paths.py` (33 tests OK).
- 2026-04-27: Fix PAYDAY 2 refresh ante Steam HTTP 403: `resolve_steam_id` ahora convierte rechazos de API key en warning accionable y prueba fallback público sin key; si el perfil público también falla, muestra error claro sin traceback crudo. Validado con `tests/test_web_assets.py -k "payday2 or Payday2SteamResolution"` (5 tests OK) y regresión relacionada `tests/test_web_assets.py tests/test_shared_web_infra.py tests/test_shared_cache_utils.py tests/test_runtime_paths.py` (31 tests OK).
- 2026-04-27: Quick win PAYDAY 2 feedback/loading: tras validación visual del usuario donde el dashboard parecía “no hacer nada”, se agrega estado global visible (`action-status`) y feedback para actualización, setup rápido, guardar config, marcar/desmarcar DLCs y acciones de bundle; se reemplazan silencios de fetch por mensajes accionables. Validado con `tests/test_web_assets.py -k "payday2"` (3 tests OK).
- 2026-04-27: Auditoría de fallback/packaging: se elimina el uso de fallbacks HTML monolíticos embebidos en `steam_deals_web.py` y `payday2_web.py`, quedando `web/steam_deals/*` y `web/payday2/*` como fuente de UI y una pantalla mínima compartida para assets faltantes. `build_desktop.py` ahora incluye `web/payday2/favicon.svg` y `web/payday2/masks/*.svg`; `desktop_doctor.py` valida esos assets en `DATA_FILES`. Evidencia: `python3 -m unittest tests.test_web_assets tests.test_generated_files_serving tests.test_shared_web_infra`, `python3 -m py_compile shared_web_infra.py steam_deals_web.py payday2_web.py build_desktop.py desktop_doctor.py` y `git diff --check` OK.
- 2026-04-27: Auditoría documental/copy: Steam Deals y PAYDAY 2 quedan alineados con API Key opcional para wishlist pública, outputs `.md/.html/.csv` opcional en PAYDAY 2 y copy `Tu Presupuesto Ideal`; se borran artefactos locales `demo_share.html` y `.venv/` tras aprobación.
- 2026-04-27: Quick win PAYDAY 2 real-data smoke parcial: `tests/test_web_assets.py -k "payday2"` OK (2 tests) y `tests/test_shared_web_infra.py tests/test_shared_cache_utils.py tests/test_runtime_paths.py` OK (19 tests). Se levantó `payday2_web.py --no-open` en `127.0.0.1:8081`; `/` sirve el dashboard, `/api/data` carga cache real con 80 DLCs, 40 poseídos y 40 faltantes, y un toggle reversible de DLC (`3188980`) cambió 40/40 -> 41/39 -> 40/40 sin dejar estado alterado. Siguiente seguimiento: abrir en browser para validar experiencia visual/cómoda y ejecutar `Actualizar datos` si se quiere refrescar red antes de cerrar el quick win.
- 2026-04-27: Quick win de UX en Share/Compartir: el modal y reportes usan `Compartir oferta`, `Compartir juegos destacados`, texto sin jerga de `payload` y feedback `¡Copiado!`; se mantiene compatibilidad `steamtools://share`. Validado con `tests/test_generator_logic.py -k "share"` (6 tests OK) y `tests/test_desktop_share.py tests/test_web_assets.py` (22 tests OK).
- 2026-04-27: Quick win de copy en Dashboard histórico: se corrigen acentos visibles (`Página`, `Salió`, `Cambió`, `histórico`) y se reemplaza copy técnico de `include_same`/`run` por lenguaje de usuario. Validado con `tests/test_track_history_flow.py tests/test_web_assets.py` (17 tests OK).
- 2026-04-22: Track Stop queda casi cerrado: la Web UI ya no duplica `Solicitando detener ejecucion...`, `/api/stop` usa estados veraces y una prueba manual en browser confirmó que el run sí se detiene. La validación equivalente en desktop se difiere al Track Desktop mientras el launcher/fallback sigue estabilizándose.
- 2026-04-22: Quick win de UX en reportes HTML: el encabezado visible pasa de `Steam Deals` a `Ofertas de Steam`, para sonar menos como nombre interno de script y más como pantalla orientada al usuario.
- 2026-04-22: Quick win de UX en la tarjeta final: `Último reporte` se renombra a `Resumen de tu última ejecución`, alineado con el lenguaje más claro del historial y la UI principal.
- 2026-04-22: Quick win de consistencia UX en histórico/resumen: se eliminan restos de inglés como `Quick compare activo`, `deals` y `Top cambios`, sustituyéndolos por copy más natural (`Comparación rápida activa`, `ofertas`, `cambios destacados`).
- 2026-04-22: Quick win de UX en histórico: cuando una ejecución no trae nombre de promo/festival, el resumen deja de mostrar `Steam Deals` y usa `Sin evento detectado`, que comunica mejor lo que pasó.
- 2026-04-21: Quick win de UX en archivos opcionales: `HLTB CSV` y `Family JSON` se humanizan a `Export de HowLongToBeat (CSV)` y `Biblioteca familiar (JSON)`, para que el usuario entienda mejor qué archivo espera cada campo.
- 2026-04-21: Quick win de UX en archivos opcionales: `Directorio de salida` pasa a `Dónde guardar los reportes` y su placeholder se vuelve más claro (`Usar carpeta actual`).
- 2026-04-21: Quick win de UX en notificaciones automáticas: los placeholders de Telegram y Discord ahora se vuelven más guiados (`Pega aquí el token del bot`, `Pega aquí el webhook del canal`) en vez de dejar ejemplos crudos poco explicativos.
- 2026-04-21: Quick win de UX en notificaciones automáticas: el placeholder de `ID del chat de Telegram` deja el ejemplo crudo y pasa a pedir la acción directamente (`Pega aquí el ID del chat o canal`).
- 2026-04-21: Quick win de UX en notificaciones automáticas: los labels de Telegram/Discord se humanizan (`Token del bot de Telegram`, `ID del chat de Telegram`, `Webhook de Discord`) para depender menos de jerga técnica cruda.
- 2026-04-21: Quick win de UX en notificaciones automáticas: `Telegram Bot Token`, `Telegram Chat ID` y `Discord Webhook` ahora explican mejor para qué sirve cada campo y dónde impacta el aviso.
- 2026-04-21: Quick win de consistencia UX: `Solo deals nuevos` se renombra a `Solo ofertas nuevas`, manteniendo el mismo comportamiento pero con lenguaje más natural para usuario normal.
- 2026-04-21: Quick win de consistencia UX en Steam Deck: estados visibles como `Verified`, `Playable` y `Unsupported` se traducen a `Verificado`, `Jugable` y `No compatible` en reportes/labels visibles.
- 2026-04-21: Quick win de UX en la antigua `Watchlist`: la sección ahora se presenta como `Alertas de precio`, aclara para qué sirve y cambia `AppID` por `ID del juego` para sonar menos técnica.
- 2026-04-21: Quick win de UX en configuración: `Fuentes de datos` pasa a `Archivos opcionales` y ahora explica mejor para qué sirven `HLTB CSV`, `Family JSON` y el `Directorio de salida`.
- 2026-04-21: Quick win de UX en configuración: `Notificaciones` pasa a `Notificaciones automáticas` y su hint ahora explica mejor que te avisa sobre ofertas nuevas o cambios de precio importantes.
- 2026-04-21: Quick win de UX en histórico: se elimina `(MVP)` del título porque era jerga interna que no aportaba nada al usuario final.
- 2026-04-21: Quick win de consistencia UX en histórico: el resumen de cada ejecución deja de mostrar `deals` y pasa a `ofertas`, manteniendo el evento/festival visible con wording más natural.
- 2026-04-21: Quick win de consistencia UX en resúmenes: el pill `nuevos` se aclara a `ofertas nuevas`, alineado con el wording de `Solo ofertas nuevas` para evitar términos demasiado ambiguos.
- 2026-04-21: Quick win de consistencia UX en resúmenes: el pill `Deck Verified` se humaniza a `verificados para Steam Deck`, alineado con el resto del lenguaje ya traducido.
- 2026-04-21: Quick win de consistencia UX en reportes/tablas: `Steam Deck / Linux` y `Steam Deck` se simplifican hacia `Compatibilidad`, y el dashboard resume mejor la sección como `Compatibilidad Steam Deck y Linux`.
- 2026-04-21: Quick win de UX en reportes: `Tipo de juego` ahora se acompaña de una nota breve para aclarar que resume si el juego es solo, cooperativo, PvP o multijugador.
- 2026-04-21: Quick win de consistencia UX en reportes/tablas: `Deck`/`Deck/Linux` y `Modo` se humanizan hacia `Steam Deck` / `Steam Deck / Linux` y `Tipo de juego`, para que los headers suenen menos técnicos y más claros.
- 2026-04-21: Quick win de consistencia UX: `MC` pasa a mostrarse como `Metacritic` en tablas/reportes visibles, evitando abreviaturas poco claras para usuario normal.
- 2026-04-21: Quick win de consistencia UX: labels visibles de `Reviews` pasan a `Reseñas` en UI/reportes principales, reduciendo la mezcla innecesaria de inglés y español.
- 2026-04-21: Quick win de lenguaje UX en histórico: `Run` se humaniza a `Ejecución` en labels, hints, títulos y mensajes visibles para que la sección suene menos técnica.
- 2026-04-21: Quick win de consistencia UX: `Top Picks` pasa a mostrarse como `Juegos destacados` también en HTML interactivo y Share HTML, alineado con la UI principal.
- 2026-04-21: Quick win de UX en configuración principal: `Top picks` se renombra a `Juegos destacados`, para sonar menos técnico y más natural para usuario normal.
- 2026-04-21: Quick win de UX en histórico: `Buscar runs` ahora añade ejemplos concretos (`2026-04-21`, `Medieval Fest`, `gaben`) para que el usuario entienda más rápido qué puede escribir ahí.
- 2026-04-21: Quick win de UX en histórico: `Filtrar estado` ahora explica mejor qué significa `Nuevo`, `Salieron` e `Iguales`, para que el usuario no tenga que deducirlo por contexto.
- 2026-04-21: Quick win de UX en filtros avanzados: los checks de Steam Deck también se humanizan en el texto (`Solo juegos que corren en Steam Deck`, `Solo juegos verificados para Steam Deck`), para que se entiendan incluso sin leer el hint adicional.
- 2026-04-21: Quick win de UX en filtros avanzados: `Precio máximo (MXN)` ahora aclara que deja fuera juegos por arriba de ese tope, para que el usuario entienda mejor cuándo conviene usarlo.
- 2026-04-21: Quick win de UX en configuración principal: `Ordenar por` ahora explica mejor que cambia el criterio de acomodo del reporte y sugiere dejar `Score` si no sabes cuál usar.
- 2026-04-21: Quick win de UX en configuración principal: `Descuento mínimo` ahora aclara que subirlo deja fuera ofertas pequeñas y concentra el reporte en descuentos más fuertes.
- 2026-04-21: Quick win de UX en filtros avanzados: `Generar CSV` ahora aclara que sirve para llevar resultados a Excel o Google Sheets, en vez de quedar como una opción técnica sin contexto.
- 2026-04-21: Quick win de UX en filtros avanzados: `Solo deals nuevos` ahora aclara que compara contra el run anterior y que, sin historial previo, puede aportar poco o nada.
- 2026-04-21: Quick win de UX en filtros avanzados: los checks de Steam Deck ahora explican mejor la diferencia entre `compatible` y `Verified`, para que el usuario normal entienda qué tanto quiere restringir los resultados.
- 2026-04-21: Quick win de UX en filtros avanzados: `Max HLTB horas` se humaniza a `Duración máxima (horas)` y ahora aclara que sirve para priorizar juegos más cortos o evitar experiencias demasiado largas.
- 2026-04-21: Quick win de UX en filtros avanzados: los filtros de reviews ahora usan labels más claros (`Reviews positivas mín.`, `Cantidad mínima de reviews`) y explican mejor qué estás filtrando y por qué puede ayudarte.
- 2026-04-21: Quick win de UX en `Top picks`: el campo ahora aclara que solo controla cuántos juegos se destacan arriba y que no cambia el total de deals del reporte.
- 2026-04-21: Quick win de UX en histórico: `Buscar runs` deja de usar términos técnicos en el placeholder (`steam_id`/`vanity`) y ahora se apoya en un hint más claro (`fecha, evento o perfil`).
- 2026-04-21: Quick win de UX en histórico: `Orden delta` ahora tiene un hint más claro para explicar cuándo conviene ver primero subidas, bajadas o cambios fuertes entre runs.
- 2026-04-21: Quick win de UX en histórico: `Incluir precios sin cambio` ahora lleva un hint directo para explicar que agrega juegos cuyo precio quedó igual entre ambos runs.
- 2026-04-21: Quick win de UX en histórico: `Run A (base)` y `Run B (comparar)` se renombran a `Run inicial` y `Run a comparar`, para que el selector suene menos técnico y más claro para usuario normal.
- 2026-04-21: Quick win de UX en histórico: la opción `Default` dentro de `Orden delta` se renombra a `Orden normal`, para que el usuario entienda mejor que no está priorizando subidas o bajadas especiales.
- 2026-04-21: Quick win de UX en `Workers de enrichment`: el hint ahora explica en lenguaje más práctico cuándo dejar `12`, cuándo probar `16` y cuándo bajar a `8`, en vez de quedarse en wording demasiado técnico.
- 2026-04-21: Quick win de UX en `Comparar con`: el hint ahora explica mejor que sirve para ver juegos en común e ideas de regalo, además de recordar que la wishlist del amigo debe ser pública.
- 2026-04-21: Quick win de UX en Top Picks: se agrega una guía breve para interpretar `Comprar ahora`, `Vale la pena` y `Solo si ya lo traías en radar`, reduciendo la ambigüedad de esas recomendaciones rápidas para usuario normal.
- 2026-04-21: Quick win de UX en reportes HTML: `Mín. histórico` ahora puede mostrar un acceso rápido `Ver tendencia` cuando existe sparkline local, para saltar visualmente al movimiento de precio del mismo juego sin buscarlo a mano.
- 2026-04-21: Quick win de UX en histórico: el botón de comparación rápida ahora se muestra apilado con su hint y cambia su copy a `Comparar 2 recientes`, evitando el look apretado/extraño que tenía al competir visualmente con el texto explicativo.
- 2026-04-21: Quick win de UX/estado en cache: la UI ahora refuerza que `Ignorar cache` arranque desmarcado incluso al recargar o volver a la página (`pageshow`), para evitar que el navegador reviva un estado visual viejo y confunda al usuario.
- 2026-04-21: Quick win de UX en archivos generados: los enlaces finales ahora distinguen entre `Abrir` (HTML) y `Descargar` (Markdown/JSON/CSV), y explican que los archivos de texto se descargan para evitar la sensación de página en blanco al pasar por `/files/...`.
- 2026-04-21: Quick win de UX en presupuesto: `Tu Presupuesto Ideal` sale de `Filtros avanzados` y pasa a mostrarse en la configuración principal como feature visible, para que no se sienta escondido como si fuera solo otro filtro.
- 2026-04-21: Rerun real con cache caliente (`Steam Medieval Fest`) deja evidencia local de cierre parcial: artifacts `.md/.html/share.html/.json` generados correctamente, `Precio original` ya visible en reportes y `Tu Presupuesto Ideal` ya expone variantes/rerolls en HTML/JSON/MD. Quedan abiertos como follow-up el share E2E manual, la diversidad real de rerolls y la inconsistencia de batching/cache (`Caché válida ... sin nuevos` pero luego `Fetching 414 juegos` con `HTTP 400` por batch). También se observa desfase en el conteo visible de pasos (`[1/11]` ... `[12/11]`).
- 2026-04-21: `--warm-cache` local ya termino correctamente con cache persistente y logs en `~/.cache/steam_deals/logs/`; el siguiente paso operativo queda reducido al rerun largo Linux para capturar `.md/.html/.csv` + cierre limpio.
- 2026-04-21: La CLI headless ya maneja mejor errores de usuario en warm-cache (`TU_VANITY_URL`, vanity invalido, wishlist privada) con mensajes accionables en vez de traceback crudo, manteniendo exit code de error y cierre correcto del log.
- 2026-04-21: README, runbook Linux y ejemplos user-facing ahora usan `gaben` como vanity publico neutral para evitar exponer vanitys personales y reducir confusiones por copy/paste.
- 2026-04-21: El flujo de share queda mucho mas alineado entre Web UI, HTML interactivo y Share HTML: payload normalizado con aliases (`price_original`/`original_price`, `min_hist`/`min_historical`), botones/modal consistentes y validacion automatica reforzada; queda pendiente solo la verificacion manual E2E final.
- 2026-04-21: Se cierran los pendientes base de `Tu Presupuesto Ideal` para variantes chica/media/grande y cambios de lista/juego; queda abierto solo el refinamiento de diversidad real del reroll/reemplazos.
- 2026-04-21: Quick win de UX en histórico: el botón `Comparar últimos 2 runs` se acorta a `Últimos 2 runs`, mejora su spacing y deja un hint más directo para que se entienda sin verse apretado.
- 2026-04-20: Quick win de UX en tendencia/histórico: el bloque `Trend` ahora usa copy más claro para usuario normal (`Tendencia general de ofertas`, `Lectura rápida`, `Volumen similar al inicio`, etc.) para explicar que resume el volumen total de ofertas y no el precio de un juego individual.
- 2026-04-20: Se cierra el pendiente de la etiqueta `Era`: HTML/Markdown/reportes relevantes ahora usan `Precio original`, alineado con el resto de la UX para evitar lenguaje ambiguo.
- 2026-04-20: Quick win de UX en `Tu Presupuesto Ideal` dentro del HTML interactivo: las variantes ahora se presentan como botones de `Rerrollear todos` debajo de la barra del presupuesto, cada juego puede mostrar `Reroll` junto al número y el modal de share usa un botón `Cerrar` consistente con el resto. Sigue pendiente revisar la diversidad real de reemplazos cuando varias opciones se sienten demasiado parecidas.
- 2026-04-20: Quick win de UX en `Tu Presupuesto Ideal`: el campo de presupuesto en Filtros avanzados ahora explica mejor que genera una lista balanceada y que el reporte puede mostrar variantes chica/media/grande y cambios de juego dentro del tope.
- 2026-04-20: Avance en share/compartir deals: se corrige el contrato del payload entre Web UI y desktop (`price_original` vs `original_price`) y el parser de `steam_tools_desktop.py` ahora tolera payload base64 URL-encoded y alias legacy; falta validación manual E2E del flujo completo para cerrar el pendiente general de share.
- 2026-04-20: El dashboard historico suma `title` explicativos en `Comparar últimos 2 runs`, `Comparar runs`, `Recargar runs` y `Restablecer filtros`, reforzando la ayuda visible ya añadida en la sección.
- 2026-04-20: La UI principal de Steam Deals ahora muestra notas breves y `title` explicativos para los botones utilitarios (`Probar config`, `Doctor desktop`, `Autofix desktop`, `Limpiar cache`, `Abrir ultimo reporte`) para que un usuario normal entienda mejor qué hace cada acción.
- 2026-04-20: Se confirma que el repo ya cuenta con artifact Linux `dist/SteamToolsDesktop`; el `.exe` historico corresponde al flujo Windows y no sirve como validacion nativa en Linux.
- 2026-04-20: Sanitizacion base del repo aplicada para reducir ruido operativo: `.tmp/`, `.pytest_cache/`, `logs/` y reportes `Steam Deals*.json` pasan a tratarse como artefactos locales no versionados; el cache real se conserva fuera de esta limpieza para no penalizar wishlists grandes.
- 2026-04-20: Sugerencia de tester CH4VE5 implementada: Top Picks y Share HTML ya etiquetan explicitamente `Score` y `Metacritic`, para que no se vea solo un numero aislado.
- 2026-04-20: Sugerencia de tester J0HNNY implementada: el paso `[3/12] Comparando wishlists...` ya muestra el nombre visible del amigo cuando se puede resolver, con fallback seguro al vanity original.
- 2026-04-20: Sugerencia de tester R0CH4 verificada/cerrada: el dashboard historico ya incluye boton `Recargar runs` para refrescar manualmente el listado tras varias ejecuciones.
- 2026-04-18: Se agrega pendiente sugerido por tester R1CK para que el `<title>` de los reportes HTML use el nombre visible del perfil de Steam en lugar de URL/steamid cuando corresponda.
- 2026-04-18: Queda implementada la mejora sugerida por tester R1CK: los reportes HTML (`.html` y `Share HTML`) y `meta.profile` en JSON ya usan el nombre visible del perfil Steam cuando está disponible, con fallback seguro al identificador original si no se puede resolver.
- 2026-04-18: Se cierra el pendiente de claridad de scoring sobre "edad": se estandariza como "antigüedad" y se documenta explícitamente que mide años desde lanzamiento.
- 2026-04-18: Se renombra "Budget Mode" a "Tu Presupuesto Ideal" en documentación, UI y reportes para hacerlo más claro para usuarios no técnicos.
- 2026-04-18: Se renombra "Steam Tags" a "Etiquetas" en la UI/reportes relevantes y documentación para mantener lenguaje más claro para usuarios no técnicos.
- 2026-04-17: Se blindó el refresh incremental por `_fetched_at`: si un fetch de precios falla o devuelve `null`, esa entrada ya no queda marcada como fresca por 24h. El siguiente run la vuelve a tratar como retryable.
- 2026-04-17: La UI compartida ahora expone `max_workers` en Filtros avanzados y los presets de Steam Deals tambien ajustan workers sugeridos (`rapido=12`, `completo=16`, `ahorro=8`) sin cambiar todavia el default global.
- 2026-04-17: El dashboard historico gano un boton para restablecer filtros y un resumen visible de Run A / Run B al usar quick compare, para que la comparacion de los ultimos 2 runs sea mas clara.
- 2026-04-17: El smoke largo Linux del desktop detecto un bug real de closeout final: `steam_deals_run_output.py` ya pasaba `smart_alerts` al resumen corto pero el wrapper compatible en `steam_deals_generator.py` no aceptaba ese argumento. Se corrigio el boundary, quedaron tests dirigidos en verde y la proxima corrida larga debe validar el cierre E2E sin crash.
- 2026-04-17: La UI compartida Steam Deals ahora permite copiar y descargar el log visible de ejecucion; se hizo para no perder tracebacks largos durante validacion manual desktop/web.
- 2026-04-17: El cache del desktop ya no se pierde en `_MEI`: se movio a ruta persistente de usuario en modo frozen y se agrego `--warm-cache` headless para precalentar `prices_cache.json` sin abrir UI. La v2 del warm-cache guarda logs automaticamente en carpeta `logs/` (o `<cache>/logs` cuando se usa ruta persistente/override).
- 2026-04-16: Alertas v2 queda en estado pendiente operativo por tiempo: aunque la implementacion v2 esta lista, falta calibracion/ejecucion manual en corrida larga real. Se agenda retomar en la proxima corrida para validar ajuste final y cerrar el item.
- 2026-04-16: Cierre Alertas v2: alertas inteligentes ahora soportan umbrales configurables de subida (`--alert-rise-pct`), margen sobre minimo global (`--alert-global-margin-pct`) y priorizacion por score minimo (`--alert-score-min`), manteniendo compatibilidad por default. Se conserva integracion en resumen final corto del run.
- 2026-04-16: Cierre documental del flujo Obsidian/Notion: README ahora incluye perfiles de frontmatter recomendados y checklist manual de validacion de import (Obsidian + Notion) para ejecucion reproducible. Queda pendiente solo la validacion E2E manual en host real para marcar cierre total del item.
- 2026-04-08: Se crea este archivo como consolidado unico de pendientes.
- 2026-04-08: Se implementan eventos estructurados base (progress/file).
- 2026-04-08: Se añade preflight y acciones rapidas de UX.
- 2026-04-08: Se fortalece parser de salida web (menos dependencia ANSI).
- 2026-04-08: Se crea base desktop opcion B (pywebview + build unificado).
- 2026-04-08: Build PyInstaller completado en Windows; generado `dist/SteamToolsDesktop.exe`.
- 2026-04-08: Smoke test basico del ejecutable Windows OK (arranca proceso y cierre controlado).
- 2026-04-08: Se agrega `smoke_test_windows.ps1` como smoke test reproducible (arranque, API local, cierre limpio).
- 2026-04-08: Smoke test Windows reproducible validado con resultado `SMOKE_OK`.
- 2026-04-08: Se agrega banner de modo (primer setup vs actualizacion) en la web UI.
- 2026-04-08: Se agregan presets de ejecucion (rapido/completo/ahorro) con aplicacion en formulario.
- 2026-04-08: Se agrega clasificacion de errores por categoria (network/config/rate-limit/encoding) con sugerencias accionables en consola UI.
- 2026-04-09: Se implementa feature Compartir Deals (URL scheme steamtools://) con modal en Web UI y HTML generado. Falta probar con datos reales (requiere VPN o config de red para Steam API).
- 2026-04-11: Se blindan handlers web en Steam Deals/PAYDAY 2 (JSON invalido -> 400, limites de payload, validacion de payload/action y errores mas accionables).
- 2026-04-11: PAYDAY 2 Web inicia separacion UI embebida: `web/payday2/index.html` externo con loader/fallback en `payday2_web.py`; spec desktop actualizado para incluir el HTML.
- 2026-04-11: Steam Deals Web migra HTML a `web/steam_deals/index.html` con loader/fallback en `steam_deals_web.py`; build desktop/spec incluyen ambos HTML externos.
- 2026-04-11: PAYDAY 2 Web extrae CSS a `web/payday2/app.css` y sirve `GET /app.css`; build desktop/spec incluyen nuevo asset.
- 2026-04-13: Se completa la extraccion de UI restante: `web/steam_deals/app.css`, `web/steam_deals/app.js` y `web/payday2/app.js`; ambos web servers sirven `/app.css` y `/app.js` con fallback a HTML inline.
- 2026-04-13: Se agrega `shared_web_infra.py` y se reutiliza en Steam Deals/PAYDAY 2 para respuestas HTTP, lectura defensiva de JSON, assets de texto, subprocess y SSE local.
- 2026-04-13: Se corrige shutdown de `payday2_web.py` para cerrar limpio con refresh activo, incluyendo terminacion del child process.
- 2026-04-13: README se actualiza para aclarar superficies, dependencias desktop y mapa de modulos/entrypoints.
- 2026-04-13: Se agregan smoke tests locales minimos en `.tmp/local-smoke-tests/` para Steam Deals Web, PAYDAY 2 Web y la ruta segura desktop `--internal-web`; quedan fuera del flujo normal de Git en este clon. `smoke_test_windows.ps1` se mantiene como smoke separado para Windows empaquetado.
- 2026-04-13: Se agregan tests unitarios puros en `tests/test_generator_logic.py` para score, filtros, matching/gift ideas y budget picks.
- 2026-04-13: Se completa el primer corte de capa shared con `shared/io_utils.py` y `shared/cache_utils.py`; Steam Deals y PAYDAY 2 ya comparten helpers de config JSON, HTTP JSON y cache reutilizable, con tests locales para los helpers shared.
- 2026-04-14: Se extrae CSV a `renderers/csv_renderer.py` manteniendo wrapper compatible en `steam_deals_generator.py`; validacion puntual OK con `py_compile`, `steam_deals_generator.py --help`, `steam_deals_web.py --help` e import seguro de `steam_tools_desktop.py`.
- 2026-04-14: Se agrega pendiente explicito para asegurar/validar fallback al navegador cuando `pywebview` no sea compatible o no pueda iniciar backend nativo.
- 2026-04-14: `steam_tools_desktop.py` ahora abre la Web UI con flag explicito de fallback y la UI compartida muestra banner/aviso cuando `pywebview` no esta disponible, se demora demasiado o falla al iniciar; falta validar este comportamiento en Linux/macOS nativos.
- 2026-04-14: Se completa el barrido de integracion del corte `renderers/` con `py_compile`, `steam_deals_generator.py --help`, `steam_deals_web.py --help`, `steam_web_smoke.py` y `desktop_smoke.py`; el primer corte de modularizacion de `renderers/` queda validado.
- 2026-04-14: Se extrae `steam_deals_recommendations.py` con `build_gift_ideas`, `compute_value_score`, `rank_top_picks` y `compute_budget_picks`; `steam_deals_generator.py` conserva wrappers compatibles y `tests/test_generator_logic.py` queda OK (9 tests).
- 2026-04-14: Se extrae `steam_deals_hltb.py` con parseo HLTB, normalizacion/matching y cruce HLTB × deals; `steam_deals_generator.py` conserva wrappers compatibles y `tests/test_generator_logic.py` queda OK (11 tests).
- 2026-04-14: Se extrae `steam_deals_filters.py` con `filter_by_genres` y `apply_filters`; `steam_deals_generator.py` conserva wrappers compatibles y `tests/test_generator_logic.py` queda OK (12 tests).
- 2026-04-14: Se extrae `steam_deals_history.py` con fallback al MD anterior, historial de runs, comparacion de deals, historial local de precios y formateo de tendencias; `steam_deals_generator.py` conserva wrappers compatibles y `tests/test_generator_logic.py` queda OK (15 tests).
- 2026-04-14: Se extrae `steam_deals_watchlist.py` con I/O, comando CLI y alertas de watchlist; `steam_deals_generator.py` conserva wrappers compatibles, `steam_deals_web.py` reutiliza la misma frontera shared y `tests/test_generator_logic.py` queda OK (22 tests).
- 2026-04-14: Se extrae `steam_deals_itad.py` con lookup, minimos historicos, mejores precios actuales y bundles activos de ITAD; `steam_deals_generator.py` conserva wrappers compatibles y `tests/test_generator_logic.py` queda OK (19 tests).
- 2026-04-14: Se extrae `steam_deals_scheduler.py` con parseo de `--schedule` y loop programado con dependencias inyectables; `steam_deals_generator.py` conserva wrapper compatible y `tests/test_generator_logic.py` queda OK (38 tests).
- 2026-04-14: Se extrae `steam_deals_steam_api.py` con resolucion de perfil/Steam ID, wishlist, owned games, compare, family JSON y oferta activa; `steam_deals_generator.py` conserva wrappers compatibles y `tests/test_generator_logic.py` queda OK (28 tests).
- 2026-04-14: Se extrae `steam_deals_notifications.py` con resumen, Telegram, Discord y dispatcher de notificaciones; `steam_deals_generator.py` conserva wrappers compatibles y `tests/test_generator_logic.py` queda OK (33 tests).
- 2026-04-14: Se extrae `steam_deals_config.py` con carga/guardado de config y boundary CLI/interactivo; `steam_deals_generator.py` conserva wrappers compatibles y `tests/test_generator_logic.py` queda OK (41 tests).
- 2026-04-14: Se extrae `steam_deals_presentation.py` con badges, grouping por tier/tag y helpers de presentacion compartidos por renderers; `steam_deals_generator.py` conserva wrappers compatibles y `tests/test_generator_logic.py` queda OK (44 tests).
- 2026-04-14: Se extrae `steam_deals_enrichment.py` con fetch paralelo, reviews, Steam Deck, ProtonDB, anti-cheat, tags y achievements; `steam_deals_generator.py` conserva wrappers compatibles y `tests/test_generator_logic.py` queda OK (48 tests).
- 2026-04-14: Se extrae `steam_deals_prices.py` con cache de precios, fetch batch con fallback individual y normalizacion de `deals`; `steam_deals_generator.py` conserva wrappers compatibles y `tests/test_generator_logic.py` queda OK (51 tests).
- 2026-04-14: Se extrae `steam_deals_run_output.py` con nombre de archivos, fallback al MD anterior, escritura de artefactos y resumen final; `steam_deals_generator.py` conserva wrappers compatibles y `tests/test_generator_logic.py` queda OK (55 tests).
- 2026-04-14: Se extrae `steam_deals_runtime_reporting.py` con symbols Unicode-safe, estilos ANSI, contrato de eventos y step/progress reporting; `steam_deals_generator.py` conserva wrappers compatibles, `steam_deals_web.py` reutiliza el mismo `EVENT_PREFIX` y `tests/test_generator_logic.py` queda OK (58 tests).
- 2026-04-15: Se completa migracion estructural incremental a `app/` con wrappers de compatibilidad en raiz (fases 1A-1D): `steam_deals_filters.py`, `steam_deals_hltb.py`, `steam_deals_recommendations.py`, `steam_deals_runtime_reporting.py`, `steam_deals_scheduler.py`, `steam_deals_run_output.py`, `steam_deals_notifications.py`, `steam_deals_presentation.py`, `steam_deals_watchlist.py`, `steam_deals_history.py`, `steam_deals_config.py`, `steam_deals_itad.py`, `steam_deals_enrichment.py`, `steam_deals_prices.py`, `steam_deals_steam_api.py`; validacion OK con `python -m py_compile` y `python -m pytest tests/test_generator_logic.py tests/test_shared_cache_utils.py` (62 passed).
- 2026-04-15: Se extrae `steam_deals_cache_policy.py` con decisiones de TTL, bypass `--no-cache`, refresh parcial/full y limpieza de archivos de cache; `steam_deals_generator.py` conserva wrappers compatibles y `tests/test_generator_logic.py` + `tests/test_shared_cache_utils.py` quedan OK (66 tests).
- 2026-04-15: Se extrae `steam_deals_enrichment_orchestration.py` con la coordinacion de reviews, Steam Deck, ProtonDB, anti-cheat, tags y achievements; `steam_deals_generator.py` conserva wrappers compatibles y `tests/test_generator_logic.py` + `tests/test_shared_cache_utils.py` quedan OK (68 tests).
- 2026-04-15: Se extrae `steam_deals_family.py` con la carga de `family_appids`, su boundary hacia HLTB y kwargs para renderers; `steam_deals_generator.py` conserva wrappers compatibles y `tests/test_generator_logic.py` + `tests/test_shared_cache_utils.py` quedan OK (70 tests).
- 2026-04-15: Se consolida `steam_deals_run_output.py` para el slice `output final` con rutas de artifacts, fan-out de escritura y closeout final; `steam_deals_generator.py` conserva wrappers compatibles y `tests/test_generator_logic.py` + `tests/test_shared_cache_utils.py` quedan OK (73 tests).
- 2026-04-15: Se extrae `steam_deals_itad_orchestration.py` con la coordinacion opcional de lookup, historical lows, current prices y bundles; `steam_deals_generator.py` conserva wrappers compatibles y `tests/test_generator_logic.py` + `tests/test_shared_cache_utils.py` quedan OK (74 tests).
- 2026-04-15: Se extrae `steam_deals_post_processing.py` con la coordinacion de `hltb_hours`, filtros y `top_picks`; `steam_deals_generator.py` conserva wrappers compatibles y `tests/test_generator_logic.py` + `tests/test_shared_cache_utils.py` quedan OK (75 tests).
- 2026-04-15: Se extrae `steam_deals_engagement_post_run.py` con la orquestacion de watchlist alerts, budget mode, gift ideas y notificaciones; `steam_deals_generator.py` conserva wrappers compatibles y `tests/test_generator_logic.py` + `tests/test_shared_cache_utils.py` quedan OK (76 tests).
- 2026-04-16: Validacion Linux local parcial: `python3`/`pip` OK, pero `pip` global del sistema quedo bloqueado por PEP 668; se crea `.venv`, se instalan deps desktop y el build local genera `dist/SteamToolsDesktop`.
- 2026-04-16: Primer arranque Linux del artefacto confirma fallback web por falta de backend nativo (`qtpy`/`gi`). Tras instalar `pywebview[qt]` y rehacer build, el artefacto vuelve a levantar server local sin log de fallback; luego se confirma ventana nativa real en sesion grafica no-root usando `runuser` + `QT_QPA_PLATFORM=xcb`.
- 2026-04-16: El smoke funcional inicial del binario congelado detecta bug real de packaging PyInstaller (`ModuleNotFoundError: shared`) cuando `steam_tools_desktop.py` ejecuta `steam_deals_generator.py` via `runpy`; se corrige `build_desktop.py` para agregar `--paths` + `hidden imports` + `collect_submodules(shared/renderers/app)`.
- 2026-04-16: Check liviano post-fix del binario congelado OK: `steam_deals_generator.py` ya emite el primer evento `progress` (`Resolviendo Steam ID...`) dentro del desktop empaquetado. El smoke funcional largo queda diferido porque la wishlist real es grande y la corrida puede tardar bastante antes de generar outputs finales.
- 2026-04-16: Se implementa primer corte de Desktop Doctor read-only en `steam_tools_desktop.py --doctor`: valida `.venv`/PEP 668, imports criticos (`steam_deals_web.py` / `steam_deals_generator.py`), stack `pywebview`/Qt en Linux, disponibilidad de `PyInstaller`, guardrails actuales de `build_desktop.py`, presencia de artefacto y sesion grafica/root; queda pendiente extenderlo a UI/autofix y mayor cobertura por OS.
- 2026-04-16: Desktop Doctor se expone tambien en la Web UI con boton `Doctor desktop` y endpoint local `POST /api/desktop-doctor`; reutiliza `desktop_doctor.py` sin duplicar checks y muestra el diagnostico en la consola existente.
- 2026-04-16: Desktop Doctor suma checks read-only adicionales por OS sin tocar la UX: Linux ahora revisa tooling host de PyInstaller (`ldd`/`objdump`/`objcopy`) y heuristicas Wayland/X11 cuando hay señal suficiente; macOS revisa PyObjC/tooling local; Windows revisa WebView2 y sesion interactiva cuando corre en ese OS.
- 2026-04-16: Desktop Doctor suma primer corte de autofix liviano y opt-in: CLI `--doctor-fix` (con `--yes`) y endpoint `POST /api/desktop-doctor/fix` / boton `Autofix desktop`. Solo crea `.venv`, instala `requirements-desktop.txt` dentro del entorno local y/o lanza build local; no toca paquetes del sistema ni configuracion persistente.
- 2026-04-16: Desktop Doctor mejora guidance por OS sin cambiar la UX base: cada `WARN/FAIL` ahora explica mejor que revisar manualmente, como separar deps Python vs nativas, cuando validar Wayland/X11 o WebView2, y como revalidar el desktop por plataforma sin convertir el doctor en instalador.
- 2026-04-16: Se agregan runbooks manuales por plataforma en `docs/runbooks/desktop-linux.md`, `docs/runbooks/desktop-macos.md` y `docs/runbooks/desktop-windows.md`; consolidan precondiciones, doctor, build, smoke, fallback y evidencia reproducible sin convertir el flujo en instalador.
- 2026-04-16: Quick wins de salida/automatizacion completados en Steam Deals: nuevo artifact `.json`, endpoint local `GET /api/latest-report`, helper para localizar el ultimo artifact en `steam_deals_run_output.py` y soporte en Web UI/fallback para abrir/copiar el ultimo JSON, abrir el ultimo Share HTML, empty state sin JSON y tarjeta-resumen del ultimo run.
- 2026-04-16: Quick wins de recomendacion/documentacion completados: Top Picks y Budget Mode ahora muestran `recommendation` + `score_reasons`; `README.md` documenta export JSON, endpoint local y ejemplos mini de automatizacion (`curl`, `jq`, Python stdlib).
- 2026-04-16: Decision operativa actualizada: sin host nativo Linux/macOS disponible en esta iteracion, P2 queda en estado parcial documentado y se prioriza avance Windows/no bloqueado (documentacion + backlog ejecutable).
- 2026-04-16: Avance Windows/no bloqueado en optimizacion de velocidad: cache de precios ahora expira exactamente al TTL (24h) y se habilita refresh incremental por entrada via timestamp `_fetched_at` (solo re-fetch de appids stale o faltantes). Se robustecio fallback individual cuando un batch devuelve null (incluyendo batch unitario). Validacion OK con pruebas dirigidas (`4 passed`, `3 passed`) y barrido ampliado de cache/enrichment (`17 passed`).
- 2026-04-16: Ajuste conservador de parallel fetching en enrichment: `MAX_WORKERS` sube de `8` a `12` (sin cambiar `rate_limit`, backoff ni fallback). Validacion de regresion OK en `tests/test_generator_logic.py -k "EnrichmentTests or PriceCacheTests"` (`17 passed`).
- 2026-04-16: Dashboard historico web MVP completado en Steam Deals: backend con endpoints `GET /api/history/runs` y `GET /api/history/compare` (con validacion de params y lectura segura de `run_*.json`), UI con selector de runs A/B + tabla comparativa de precios, filtros por estado (`changed/new/removed/same`), orden por delta (`delta_desc/delta_asc/abs_desc`) y persistencia de controles en `localStorage`. Validacion de regresion OK en `tests/test_generator_logic.py -k "History or ConfigTests or PriceCacheTests"` (`17 passed`).
- 2026-04-16: Dashboard historico web avanza a MVP+1 en UI con visualizacion resumida por estado (`changed/new/removed/same`) y bloque Top Deltas (mayores bajadas/subidas) sobre los runs comparados; se integra sin cambiar contrato backend y manteniendo filtros/orden existentes. Validacion de regresion OK en `tests/test_generator_logic.py -k "History or ConfigTests or PriceCacheTests"` (`17 passed`).
- 2026-04-16: Dashboard historico web avanza a MVP+2 con tendencia temporal simple en UI usando `GET /api/history/runs` (linea + puntos de `deal_count` sobre ultimos runs, con resumen min/max y delta neto vs primer run). Se mantiene implementacion frontend-only (sin cambio de contrato backend) y convive con comparativa A/B, filtros y orden existentes. Validacion de regresion OK en `tests/test_generator_logic.py -k "History or ConfigTests or PriceCacheTests"` (`17 passed`).
- 2026-04-16: Dashboard historico web avanza a MVP+3 con navegacion historica avanzada en UI: busqueda de runs, quick compare de ultimos 2 runs y paginado simple en selectores Run A/Run B para mejorar usabilidad con historiales largos. Se mantiene el contrato backend y convive con comparativa A/B, filtros, orden, resumen por estado, Top Deltas y tendencia temporal simple. Validacion de regresion OK en `tests/test_generator_logic.py -k "History or ConfigTests or PriceCacheTests"` (`17 passed`).
- 2026-04-16: Avance parcial export Obsidian/Notion: Markdown ahora soporta frontmatter YAML base y activacion por CLI via `--md-frontmatter` (pipeline de output integrado y tests dirigidos de config/markdown en verde). Queda pendiente cerrar guia de uso/plantillas y validacion de importacion end-to-end en Obsidian/Notion.
